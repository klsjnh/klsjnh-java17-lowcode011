# 015 — 项目信息（Project Info）

> 项目事实卡：定位 / 技术栈 / 骨架 / 构建与运行 / 当前能力。版本与键的事实来源是父 `pom.xml` 与 [015.topic-config](infrastructure011/015.topic-config.md)。

## 011.定位

`klsjnh-java17-framework011` —— Java 17 纯血 DDD 技术底座，Maven 多模块，供第三方业务系统 Maven 依赖引用。对外契约（响应信封 / 状态码 / 路由风格 / 公共表结构）稳定，见 [013.api-contract](013.api-contract.md)。

## 013.技术栈

| | |
|---|---|
| Java | 17 (LTS) |
| Spring Boot | 3.4.5（jakarta 体系） |
| MyBatis-Plus | 3.5.9（spring-boot3-starter） |
| 数据库 | MySQL（本地 docker 5.7.26 实测）· Druid 1.2.23（动态数据源） |
| 鉴权 | JJWT 0.12.6（HS256）· spring-security-crypto（bcrypt） |
| 调度 | Quartz 2.5.0（RAMJobStore，spring-boot-starter-quartz） |
| 对象存储 | MinIO SDK 8.5.7（S3 兼容厂商适配基础） |
| API 文档 | knife4j 4.5.0（OpenAPI3 jakarta）+ springdoc 2.8.9 |

## 015.Maven 骨架

```
com.klsjnh:java17-framework011:1.0.0（父 POM，packaging=pom）
├── java17-common011          共享内核（枚举 / 信封 / 异常 / 分页 / 常量 / 工具）——零 Spring
├── java17-domain011          纯内核（聚合 / 值对象 / 仓储接口 / Port）——禁框架
├── java17-application011     用例编排 + @Transactional
├── java17-infrastructure011  持久化基座家族 + 业务持久化 + 技术适配（JWT / bcrypt / 审计记录器 / Quartz / 动态数据源 / 存储）
├── java17-web011             Controller / 转换器 / 全局异常 / 鉴权过滤器 / Swagger 分组
└── java17-app011             唯一 main + 配置 + 参考样板（demo11 纵切面 / Demo011Scheduler）
```

## 017.构建与运行

```bash
./script011.sh gate      # 门禁：编码规范（正则 + AST）+ mvn 离线编译
./script011.sh           # gate → .vf 版本自增 → commit（ver 0.0.N）→ push
./script011.sh build011  # mvn clean package install
./script011.sh dev011    # 启动应用：杀进程后直接执行现有 jar（debug 模式）
./script011.sh dev013    # 启动应用：杀进程后重新编译（clean 删旧 jar）再执行
./script011.sh stop|restart|status|log   # 运行态管理
java -jar java17-app011/target/java17-app011-1.0.0.jar   # 11160 端口
```

- 端口 11160；接口文档 `http://127.0.0.1:11160/doc.html`（分组：系统管理011 / 存储中心 / 数据服务011 / AI模型接入011 / 低代码核心011）
- Druid 监控台 `/druid`（仅 dev 启用，klsjnh/klsjnh；⚠ allow 未配置默认只放 127.0.0.1，LAN 访问需显式配 allow，见配置体系文档）
- profile：development（默认，共享配置入库）/ production（部署机自建，不入库）；`krt.status` 见配置体系文档
- 环境依赖：Node 18+（门禁 AST：tree-sitter / tree-sitter-java，用户级 node_modules）· MySQL（docker 容器，宿主 33306）
- **容器化部署**（docker compose / ubuntu 26.04 基镜像 / `deploy/`）见 [infrastructure011/020](infrastructure011/020.topic-deploy-docker.md)

## 019.当前能力（截至 2026-09-13）

- 持久化：BasePo 四件套 + 仓库基座五层家族（含 021 主子表）+ 审计自动填充（时间 + 操作人）
- IAM：用户（双登录 + roles / 启停 / 改密 / 登出审计）/ 角色（内置保护 + 授权菜单 toggle）/ 用户角色（toggle）/ 菜单树 + selectUserMenuTree（内置角色全量旁路）/ 用户审计（REQUIRES_NEW append-only + 分页查询）
- 调度：julyScheduler 全链路（Quartz 内存模式，JobHandler 注册器，重启自愈，Demo011Scheduler 30s 演示）
- 动态数据源：yaml ci011 懒加载 + SqlRoutingPort 读写 / 方言分页（clamp [10,500]）/ 标量糖 selectStr / selectInt
- 存储中心：ObjectStoragePort（对象 + 桶 CRUD + stat）local011 / minio011 适配器，厂商分期
- 配置管理：july_config（运行时键值参数，每次查库，getByCode 程序读取入口）
- 组织机构：july_organization（树 / 层级维护 / 防环移动 / 人数角标 / 删除双约束）
- 平台导出：注册制 ExportProvider + json/csv + 500 行上限 + EXPORT 审计
- 全局异常 → 统一信封（HTTP 与 statusCode 同步，errorMessage 仅 debug）
- 参考样板：`com.klsjnh.demo11` DDD 纵切面（第三方照抄入口）

## 021.文档地图

| 主题 | 文档 |
|------|------|
| 协议全集 / 编号规则 | [011.agreements](011.agreements.md) |
| 架构 / 结构 / 配置 / 持久化 / 动态数据源 / IAM / 存储 | [infrastructure011/](infrastructure011/) |
| 业务设计与技术方案 | [requirement011](requirement011/) · [requirement013](requirement013/) |
| 编码规则 | [016.coding-standards](016.coding-standards.md) |
| 技术债红线 | [017.tech-debt-redlines](017.tech-debt-redlines.md) |
| 项目门牌 | [../Agent.md](../Agent.md) · 协议全集 [011.agreements](011.agreements.md) |
