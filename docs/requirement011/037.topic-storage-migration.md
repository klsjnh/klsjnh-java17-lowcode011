# 037.topic-storage-migration（存储中心管理面 + 在线编辑 · 迁移）

> 阶段：**requirement011（业务设计与业务要求）**。
> 背景：老项目 `kj17-f016_20260719_a011`（存储中心）拥有完整**管理面**（存储实例 / 桶 / 对象）与**在线文本编辑**；本项目（java17-framework011）只迁了「`ObjectStoragePort` + local011/minio011 双适配器」的**端口层**，管理面未迁。
> 用户诉求（2026-09-15）：**找老代码写方案迁移**。
> 技术方案：[requirement013/037.topic-storage-migration](../requirement013/037.topic-storage-migration.md)
> 架构底册：[infrastructure011/019.topic-storage-center](../infrastructure011/019.topic-storage-center.md)

## 011.对象定义（ObjectInfo011）

| param011   | param013                                  | remark011 |
| ---------- | ----------------------------------------- | --------- |
| objectCode | krt.storage-center（yaml 引导）+ july_storage（运行时真源） | |
| objectName | 存储中心 - 存储实例 / 桶 / 对象管理 + 在线编辑 | URL 前缀 `/klsjnh/storagecenter/**` |
| objectUrl  | /klsjnh/storagecenter/julyStorage/v1（实例+桶）· /klsjnh/storagecenter/julyObject/v1（对象） | Swagger 组 `storagecenter` |

## 013.能力对照（老 f016 → 本项目现状）

| # | 能力 | 老项目 | 本项目现状 | 迁移 |
|---|------|--------|-----------|------|
| 1 | 配置来源 | **表驱动 `july_storage` 多实例**，按 storageCode 解析并缓存 | yaml 单实例 `krt.storage-center.default-type`（`@ConditionalOnProperty` bean） | ✳ 改造为表驱动 + yaml 播种 |
| 2 | 存储实例管理 | `JulyStorage` CRUD + `testConnection` | 无 | ✳ 新增 |
| 3 | 桶管理 | list / page / getByName / insert(region) / remove / test | 端口有 bucket ops（存在/建/删/列），**无 HTTP** | ✳ 新增应用+web |
| 4 | 对象管理 | list / page / stat / upload / download / remove / batchRemove / presignedUrl | 端口有 get/put/delete/stat/list，**无 HTTP** | ✳ 新增应用+web（presign 见边界） |
| 5 | **在线文本编辑** | `readText` / `saveText` + `EditableTextPolicy`（sql / markdown，≤1MB） | 无 | ✳ 迁 domain 策略 + 应用/端点 |
| 6 | provider 抽象 | LOCAL / S3，`ObjectStorageAdapterFactory` 造 adapter | `StorageType011` 六值（local011/minio011/cos011/tos011/oss011/s3011），每厂商 bean | ✳ 统一到 `StorageType011` + 工厂 |
| 7 | 启动播种 | `StorageCenterBootstrapService/Runner`（yaml → 表） | 无 | ✳ 新增（对齐 031 seed 口径） |
| 8 | 预签名 URL | `presignedGetUrl` | 端口无 | ⏸ 见 §021 |

## 015.迁移范围

**做（本期）**：`july_storage` 表驱动多实例 + 实例 CRUD/testConnection + 桶 CRUD + 对象 列表/分页/stat/上传/下载/删除/批删 + 在线编辑（readText/saveText）+ yaml 播种 + 适配器工厂（按实例配置造 adapter）。

**不做（边界外）**：预签名 URL 直传、分片上传/断点续传、桶生命周期/配额/策略、流式大文件（本项目端口为 `byte[]`，大文件见二期）、各厂商原生 SDK（cos011/tos011/oss011/s3011 仍分期）、前端页面。

## 017.业务规则

- `storageCode` 全表唯一、**不可变**；实例 `provider` 取值限 `StorageType011`；
- `local011` 必填 `basePath`；S3 系（minio011/s3011/cos011/tos011/oss011）必填 `endpoint` + `accessKey` + `secretKey`；
- **`accessKey` / `secretKey` 出参打码 `******`**（对齐老 f016 口径，2026-09-17 修订）；
- 停用实例（status=0）不可被解析，调用即 400；
- **在线编辑**：目标对象 ≤ **1MB**（`EditableTextPolicy.MAX_SIZE_BYTES`），UTF-8；编辑器类型按扩展名判定（`.sql`→sql 编辑器，其余→markdown）；
- 对象/桶为**物理存在**（不落业务表）；实例为**逻辑删除**。

## 019.接口清单（前缀 /klsjnh/storagecenter）

**实例 `/storage`**：insert / update / logicDelete / logicDeleteBatch / getById(GET) / getByCode(GET) / selectListByPage / testConnection

**桶 `/bucket`**：selectList / selectListByPage / getByName(GET) / insert / remove / testConnection

**对象 `/object`**：selectList / selectListByPage / stat(GET) / upload / download(GET) / remove / batchRemove / **readText(GET)** / **saveText**

## 021.边界外（本期不做）

- 流式 `InputStream` 端口形态（大文件，019 §021 二期已列）；
- 分片上传 / 断点续传 / 秒传；
- 桶生命周期 / 配额 / 策略 / 跨域；
- 厂商原生 SDK 适配器（分期）；
- 在线编辑的多用户协同 / 版本历史。

## 023.决策记录（2026-09-15 全部定案）

| # | 决策点 | 结论 |
|---|------|------|
| 1 | 配置模型 | ✅ **表驱动多实例 + yaml 播种**（对齐 031「表为真源」） |
| 2 | provider 口径 | ✅ 用本项目 `StorageType011`（local011/minio011/…），不用老 LOCAL/S3 |
| 3 | `july_storage` 带 `sort_order` | ✅ **带**（`BasePo011`） |
| 4 | 预签名 URL | ✅ **本期做**（local→`file:` URI；S3/minio→真实预签名） |
| 5 | 端口形态 | ✅ 维持 `byte[]`，大文件流式列二期 |
| 6 | 在线编辑 | ✅ sql/markdown 两种 + 1MB 上限 |
