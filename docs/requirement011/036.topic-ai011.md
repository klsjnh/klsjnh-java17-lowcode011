# 036.topic-ai011（AI 模型接入）

> 阶段：**requirement011（业务设计与业务要求）**。
> 决策定案（2026-09-15）：新域 `ai011`；主子表 `july_ai_model_provider` + `july_ai_model_provider_api`；主表/子表**都带排序**；`api_key` 出参**不回显**（VO 类型层无该字段）；本期明文入库，加密留二期。

## 011.对象定义（ObjectInfo011）

| param011   | param013                                                  | remark011 |
| ---------- | --------------------------------------------------------- | --------- |
| objectCode | july_ai_model_provider（主）/ july_ai_model_provider_api（子） | 主子表 |
| objectName | AI 模型接入 - 提供商管理                                    | 新域 `ai011` |
| objectUrl  | /klsjnh/ai011/julyAiModelProvider/v1                      | URL 按现行框架口径 |

**归属说明**：AI 模型接入是独立能力域（模型提供商 + 密钥注册），与 `system011`（系统管理）、`dataservice011`（数据服务）平级，故新起顶层域 `ai011`；后续 chat / embedding 能力从该域读提供商与密钥。

## 013.字段定义（继承 BasePo 公共列）

**主表 `july_ai_model_provider`**

| fieldCode    | fieldName  | fieldType | fieldLength | requiredField | defaultValue |
| ------------ | ---------- | --------- | ----------- | ------------- | ------------ |
| providerCode | 提供商编码 | String    | 60          | true          | -            |
| providerName | 提供商名称 | String    | 100         | true          | -            |
| baseUrl      | Base URL   | String    | 300         | true          | -            |
| models       | 模型清单   | String    | 500         | false         | -            |
| sortOrder    | 排序       | Integer   | -           | false         | 9999         |
| status       | 状态       | String    | 3           | false         | '1'          |
| remark       | 备注       | String    | 300         | false         | -            |

**子表 `july_ai_model_provider_api`**

| fieldCode | fieldName | fieldType | fieldLength | requiredField | defaultValue |
| --------- | --------- | --------- | ----------- | ------------- | ------------ |
| pkMt      | 主表链接  | String    | 33          | true          | -            |
| apiCode   | 密钥编码  | String    | 60          | true          | -            |
| apiName   | 密钥名称  | String    | 100         | true          | -            |
| apiKey    | API Key   | String    | 300         | true          | -            |
| sortOrder | 排序      | Integer   | -           | false         | 9999         |
| status    | 状态      | String    | 3           | false         | '1'          |
| remark    | 备注      | String    | 300         | false         | -            |

> 链接列按 [016 §7](../016.coding-standards.md) 主子表规约走 `pk_mt`（Java `pkMt`，`AiModelProviderApiPo implements MasterLinked`）。
> 主表/子表 PO 均继承 `BasePo011`（`BasePo` + `sort_order`）。

## 015.定位边界

| 对象 | 放什么 | 边界 |
|------|--------|------|
| july_ai_model_provider（本对象） | 一个厂商的接入点：编码 / 名称 / baseUrl / 模型清单 | 一厂商一行，`provider_code` 唯一 |
| july_ai_model_provider_api（本对象子表） | 同一厂商下的多个 API Key（多账号 / 额度） | 一密钥一行，`(pk_mt, api_code)` 唯一 |
| july_config（029） | 运行时键值参数 | 不放密钥（密钥是模型接入域资产） |
| july_datasource（031） | 业务库连接 | 二者互不隶属 |

## 017.业务规则

- `providerCode` 全表唯一（唯一索引兜底 + 保存预检），**不可变**（修改只改名称/baseUrl/models/状态/备注）；
- 子表 `apiCode` 在**同一提供商内唯一**（唯一索引 `(pk_mt, api_code)`）；不同提供商可重名；
- 排序：主表、子表均按 `sort_order` **升序**（同值按 id）；
- `api_key` **出参不回显**：`AiModelProviderApiVo011` 类型层**不含**该字段；入库为明文（加密存储二期，`KrtConfig011` 预埋密钥位）；
- `status='0'` 停用：程序读入口不可见（停用提供商 → 无密钥返回；停用密钥 → 不返回）；
- **密钥选取**（程序调用口径）：`getDefaultApi(code)` 取该提供商下**第一个启用密钥**（sort_order 升序）；多密钥轮询 / 指定留后续；
- **删除约束**：删除提供商时若仍存在存活密钥 → `400`（先清密钥）；密钥可单条逻辑删除；
- 程序读入口（UseCase 方法，**无 HTTP 端点**）：`getByCode` / `getApiList` / `getDefaultApi`；
- `testConnection`：以 `{base_url}` + `/models` 发 GET（`Authorization: Bearer <默认密钥>`），返回连通结果（HTTP 恒 200，结果在载荷内），**不改状态、不落库**。

## 019.接口清单（管理面，统一信封）

**提供商主表**（前缀 /klsjnh/ai011/julyAiModelProvider/v1）

| 端点 | 说明 |
|------|------|
| /insert | 新增提供商（providerCode 查重） |
| /update | 修改（名称/baseUrl/models/状态/备注/排序；code 不可变） |
| /logicDelete | 逻辑删除（受删除约束） |
| /getById | 主键查询（GET） |
| /selectListByPage | 分页查询（code/name 模糊 + 状态过滤） |

**密钥子表**

| 端点 | 说明 |
|------|------|
| /insertApi | 新增密钥（同提供商内 apiCode 查重；api_key 必填） |
| /updateApi | 修改密钥（apiCode 不可变；api_key 留空保持原值） |
| /logicDeleteApi | 逻辑删除密钥（单个） |
| /selectApiListByProvider | 按提供商取密钥列表（排序 + 状态过滤，**不含 api_key**） |

**其它**

| 端点 | 说明 |
|------|------|
| /testConnection | 探测提供商（GET baseUrl/models，用默认密钥） |

程序读取（无 HTTP）：`getByCode` / `getApiList` / `getDefaultApi`（见 §017）。

## 021.边界外（本期不做）

- `api_key` 加密存储 / 解密（二期，预埋 `KrtConfig011` 密钥位）；
- 多密钥轮询 / 负载均衡 / 失效熔断；
- 模型级子表（本期 `models` 为逗号分隔字符串）；
- 缓存层（每次查库，对齐 029 / 031）；
- 030 子表密钥导出（主表 `julyAiModelProvider` 已接导出；`api_key` 因敏感未导，待脱敏口径）；
- 调用侧 chat / embedding 业务能力。

## 023.决策记录（2026-09-15 已定案）

| # | 决策点 | 结论 |
|---|------|------|
| 1 | 域归属 | ✅ 新顶层域 `ai011` |
| 2 | 表结构 | ✅ 主子表（july_ai_model_provider + july_ai_model_provider_api） |
| 3 | 排序 | ✅ 主表、子表都带 sort_order（BasePo011） |
| 4 | 子表名 | ✅ july_ai_model_provider_api |
| 5 | models | ✅ 逗号分隔字符串（step-3.7-flash, step-router-v1） |
| 6 | 密钥选取 | ✅ 默认取第一个启用密钥（sort_order 升序） |
| 7 | api_key 回显 | ✅ 出参不回显（类型层剔除）；明文入库，加密二期 |
| 8 | testConnection | ✅ 要（GET baseUrl/models） |
| 9 | 删除约束 | ✅ 有存活密钥拒删（400） |
