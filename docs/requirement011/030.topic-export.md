# 030.topic-export（数据导出 · 平台功能）

> 阶段：**requirement011（业务设计与业务要求）**。决策定案（2026-09-13）：导出是**平台功能**——注册制 Provider，非逐对象手写端点。

## 011.对象定义（ObjectInfo011）

| param011   | param013                        | remark011 |
| ---------- | ------------------------------- | --------- |
| objectCode | export                          |           |
| objectName | 数据导出（平台功能）              |           |
| objectUrl  | /klsjnh/system011/export/v1     |           |

## 013.请求定义

| fieldCode  | fieldName | fieldType | fieldLength | requiredField |
| ---------- | --------- | --------- | ----------- | ------------- |
| objectCode | 导出对象  | String    | 60          | true          |
| format     | 导出格式  | String    | 10          | true          |

## 015.平台机制（注册制，同 JobHandler 模式）

```
java17-common011
  common.enums.ExportFormat011                    JSON("json") / CSV("csv")
java17-application011
  application.export.ExportProvider               契约：objectCode() + exportRows() → List<Map>（列键有序）
  application.export.ExportProviderRegistry       收集容器内全部 Provider Bean（同 JobHandlerRegistry）
  application.export.ExportUseCase                解析 Provider → 序列化 → EXPORT 审计；行上限 500 代码限死
  application.system011.<域>.<对象>ExportProvider  各业务对象注册（现已 5 个：user / role / menu / config / organization）
java17-web011
  web.system011.controller.ExportController       POST 二进制下载（Content-Disposition；绕过 JSON 信封属设计）
```

- **上限 500 行**（代码限死，越界截断 + WARN）——沿老积累导出铁律；
- **敏感列红线**：Provider 不得携带 password 哈希等敏感列（julyUser Provider 已示范排除）；
- **必须登录**：无 operator → 401（debug 态同样要求——导出是敏感行为）；
- EXPORT 审计：object_code = 对象编码，content 记录"实际导出 / 总行数"；
- 新对象要可导 = 写一个 Provider Bean 注册即生效，平台零改动。

## 017.格式

- JSON：pretty-print + JSR-310 时间可读；
- CSV：UTF-8 BOM（Excel 直开）+ 表头 + 标准转义（逗号/引号/换行）。

## 021.边界外（本期不做）

- Excel(xlsx) 原生格式（需 POI，二期）；
- 异步导出 / 大数据量流式导出 / 导出任务中心；
- 按过滤条件导出（本期全量 alive 行，上限 500）。

## 023.决策记录（2026-09-13 已定案）

| # | 决策点 | 结论 |
|---|------|------|
| 1 | 形态 | ✅ 平台功能：注册制 Provider + 统一端点 |
| 2 | 上限 | ✅ 500 行代码限死 |
| 3 | 审计 | ✅ 每次导出发射 EXPORT 审计 |

## 025.E2E 记录（2026-09-13 全过）

julyUser JSON（3 行、无 password 列、文件名 julyUser_时间戳.json）· CSV（BOM + 中文正常）· 未知对象 400 · 无 token 401 · EXPORT/LOGOUT 审计落库。
