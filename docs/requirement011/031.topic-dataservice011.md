# 031.topic-dataservice011（数据源管理）

> 阶段：**requirement011（业务设计与业务要求）**。
> 新域 **dataservice011** 的起点主题。范围定案（2026-09-15）：**本期只做 `july_datasource` 表驱动 + CRUD + 测试连接**。
> 上游架构：[infrastructure011/017.topic-dynamic-datasource](../infrastructure011/017.topic-dynamic-datasource.md)（动态数据源横切设计，本文与之互引不复制）。
> 参照实现：`D:\aikbjava011\java17011\kj17-f011_20260718_a011`（老项目完整三表版，含 `july_datasource` / `july_sql` / `july_model`）。

## 011.对象定义（ObjectInfo011）

| param011   | param013 | remark011 |
| ---------- | -------- | --------- |
| objectCode | july_datasource | 表名即对象码 |
| objectName | 数据服务 - 数据源管理 | 新域 dataservice011 的首个对象 |
| objectUrl  | /klsjnh/dataservice011/julyDatasource/v1 | URL 按现行框架口径 |

## 013.字段定义（fieldList，继承 BasePo 公共列）

继承 BasePo 公共列：`id` / `status` / `create_by` / `update_by` / `create_time` / `update_time` / `dr`（见 [base-entity-columns.sql](../sql/base-entity-columns.sql)）。

| fieldCode   | fieldName   | fieldType | fieldLength | requiredField | defaultValue |
| ----------- | ----------- | --------- | ----------- | ------------- | ------------ |
| dsCode      | 数据源编码  | String    | 60          | true          | -            |
| dsName      | 数据源名称  | String    | 100         | true          | -            |
| dbType      | 数据库类型  | String    | 20          | true          | mysql        |
| jdbcUrl     | JDBC URL    | String    | 500         | true          | -            |
| schemaName  | 库名 / Schema | String  | 60          | false         | -            |
| username    | 用户名      | String    | 100         | false         | -            |
| password    | 密码        | String    | 300         | false         | -            |
| driverClass | 驱动类名    | String    | 200         | false         | -            |
| poolConfig  | 连接池 JSON | String    | 500         | false         | -            |
| remark      | 备注        | String    | 300         | false         | -            |

**与老项目的列差异（有意收敛，见 §017）**：老项目用 `create_date` / `modify_date` / `created_by` / `updated_by` 四列自定义审计名，本项目统一走 **BasePo 公共列**（`create_time` / `update_time` / `create_by` / `update_by`）；老项目 `status varchar(3)` 保留（`Status011` 口径）。

## 015.定位与边界（与 krt.ci011 的分工）

| 数据源来源 | 放什么 | 生效方式 |
|------------|--------|----------|
| **krt.ci011**（yaml，KrtConfig011） | **一次性种子**：启动时把内容播种进 `july_datasource` | 仅在表里没有该 `dsCode` 时插入一次 |
| **july_datasource（本对象）** | **运行时唯一真源**：新增库、改密码、启停、删除 | 改库 + 重载注册表即生效 |

**播种规则（2026-09-15 定案，取代早期"只允许一个家"的红线）**：启动由 `DatasourceSeedRunner` 逐条比对 `dsCode`：

| 表中状态 | 处理 |
|----------|------|
| 没有该 `dsCode` | INSERT（种子落地） |
| 有该 `dsCode` | 跳过，**不更新不复活**（判重含 `dr='1'` 墓碑行，故管理页删掉的行不会被重启复活） |
| 有但内容不一致（dsName / jdbcUrl / dbType） | **表赢**，记 WARN 列出差异字段，不写库 |

**失败策略**：单条失败只记 ERROR（含 `dsCode` + 异常堆栈）后继续，**绝不抛异常阻断启动** —— 服务必须能起来，人进管理页手工修。无「播种开关」，全自动执行。

## 017.业务规则

- `dsCode` 全表唯一（唯一索引兜底 + 保存预检），**`dsCode` 不可变**（修改只改连接参数）；
- `dsCode` 保守取值校验：字母 / 数字 / 下划线 / 连字符，长度 1–60（它同时是连接池名与日志标识）；
- `dsCode` **保留字**：`master`（主库标识）不可占用 —— 与老项目 `JulyDatasource.validateCode` 口径一致；
- `dbType` 取值限 `DatabaseType011` 枚举四值：`mysql` / `oracle` / `sqlserver` / `postgresql`（接受 `postgres` 别名）；未知值即 400；
- `driverClass` 可空：**为空时按 `dbType` 取 `DatabaseType011.getDriverClassName()`**（枚举是驱动类名唯一真源，禁止在业务代码硬编码驱动字符串）；
- `status`：`1` 启用 / `0` 停用；**停用即从运行时注册表摘除**，路由到该 `dsCode` 抛 400；
- `jdbcUrl` 必填且以 `jdbc:` 开头（保存预检，不做深度解析——方言差异太大）；
- **删除用逻辑删除**（`dr`），不做物理删除；不提供物理删除端点（属基础设施维护动作，见 [016.coding-standards §6.3](../016.coding-standards.md)）；
- **密码不回显**：`getById` / `selectListByPage` 等一切出参**不得返回 `password` 字段**（老项目明文回吐，是安全隐患，本期修正）；仅 `testConnection` 可在请求侧传入明文。

## 019.接口清单（管理面，统一信封）

前缀 `/klsjnh/dataservice011/julyDatasource/v1`。动作命名与 HTTP 方法严格遵循 [016.coding-standards §6](../016.coding-standards.md)。

| 端点 | 方法 | 入参 | 说明 |
| ---- | ---- | ---- | ---- |
| `/insert` | POST | `JulyDatasourceSaveVo011` | 新增（`dsCode` 查重 + 保留字校验） |
| `/update` | POST | `JulyDatasourceUpdateVo011` | 修改（`dsCode` 不可变） |
| `/logicDelete` | POST | `IdVo011` | 逻辑删除（单条） |
| `/logicDeleteBatch` | POST | `IdsVo011` | 逻辑删除（批量，`BatchDeleteResultVo011` 回报） |
| `/getById` | **GET** | Query `id` | 主键点查（密码脱敏） |
| `/getByCode` | **GET** | Query `code` | 按 `dsCode` 点查（密码脱敏） |
| `/selectList` | POST | `JulyDatasourceQueryVo011` | 条件列表 |
| `/selectListByPage` | POST | `JulyDatasourcePageQueryVo011` | 分页查询（`dsCode` / `dsName` / `dbType` / `status` 筛选） |
| `/testConnection` | POST | `JulyDatasourceConnectVo011` | 连接探测（**支持草稿态**：未保存的配置也可测） |
| `/reloadRegistry` | POST | 无 | 重载运行时注册表（表 → 池） |

**端点数量与老项目的差异**：老项目 10 个端点（含 `batchLogicDelete` 命名违规），本期按现行门禁改为 `logicDeleteBatch`（动词打头），并补 `getByCode` 的 GET 语义修正。

## 021.边界外（本期不做）

- **`july_sql`（SQL 语句注册）与 `july_model`（业务建模 + 版本）** —— 老项目已有完整实现，属**二期**；
- **基于 `july_sql` 的对外数据服务端点**（动态 SQL 执行 + 参数化 + 分页）—— 现有 `SqlRoutingPort` 保留，但不接管理面；
- 读写分离、负载均衡、多租户路由；
- 分布式事务（动态库只跑业务查询 / DDL，不参与主库事务）；
- 密码加密存储（本期沿用 yaml / 环境变量口径，加密方案二期随 IAM 密钥体系一并落）；
- 连接池参数的可视化调参（`poolConfig` 列预留但本期不解析，走代码默认值）。

## 023.决策记录（2026-09-15 已定案）

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 域归属与新 URL 前缀 | ✅ 新域 **dataservice011**，前缀 `/klsjnh/dataservice011/julyDatasource/v1`（老项目 `/klsjnh/datasource011/datasource` 弃用） |
| 2 | 本期范围 | ✅ 仅 `july_datasource` 表驱动 + CRUD + 测试连接；`july_sql` / `july_model` 由 [033](033.topic-business-modeling.md) 承接（合并为一张 `july_business_modeling`） |
| 3 | 加密密钥来源 | ✅ **由环境变量注入**（沿用 `015.topic-config`「同一配置一个家」口径）；密钥本体不入库、不入文档 |
| 4 | 密码出站策略 | ✅ **不回显**：所有出参剔除 `password`；仅 `testConnection` 请求侧可传明文 |
| 5 | 密钥存储引擎/路径 | ✅ **跟随现有配置体系，不新造轮子** —— 密钥经环境变量进入 `KrtConfig011`（`krt.*` 绑定），业务代码禁 `@Value` 的红线不变 |

### 023.1 决策 3/5 补充说明（密钥口径）

用户定案原文：「加密密钥来源 由环境变量 存储引擎/路径 跟随现有配置体系」。落地含义：

- **不新增密钥存储介质**（不引入 Vault / 不新增密钥表 / 不写死配置文件）—— 复用 `KrtConfig011` 作为唯一框架配置入口；
- `KrtConfig011` 新增 `dataservice011` 配置组，持一个 `secretKey`（String，无默认值）；
- 生产环境经环境变量注入（与现有 `krt.jwt.secret` 完全同构的做法）；
- **但是**：本期 §021 已明确「密码加密存储」在边界外 —— 故本决策**同期只预埋配置位**，不实现加解密。二期做加密时，密钥已就位，无需再改配置体系。
