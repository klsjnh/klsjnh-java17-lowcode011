# 2026-09-15 原始对话记录

> 脱敏说明：本文不含凭据 / 内网 IP / 密钥 / token。

## 会话一：规划 dataservice011 域（13:33 — 13:50）

**用户诉求（原文）**

> 规划一个新的 域
> 叫 dataservice011
> 有个东西叫 juydatasouce
> D:\aikbjava011
> D:\Source\java8
> D:\Source\java17
> 老项目应该有 动态数据源的管理 现在系统里也有一部分你先看看 出个方案

**助手调研动作与发现**

1. 三个根目录（`D:\aikbjava011` / `D:\Source\java8` / `D:\Source\java17`）逐一枚举子项目。
2. 全盘搜索 `juydatasouce` 及其拼写变体（`juyDataSource` / `juy_datasource` / `juyDatasource`）——**字面名不存在**。git pack 内几处 `juy` 命中经上下文提取确认为二进制噪声，非真实标识符。
3. 判定 `juydatasouce` 为 `july_datasource` 的口头简写，并定位实体所在：
   - **老项目（完整三表实现）**：`D:\aikbjava011\java17011\kj17-f011_20260718_a011`，包名即 `datasource011`，控制器路由 `/klsjnh/datasource011/datasource`。
   - **老框架（更早形态）**：`D:\aikbjava011\java17011\java17-klsjnh-framewor011`（`klsjnh-runtime011`），有 `DynamicDataSource` / `DynamicDataSourceUtil` / `DataSourceDataImportHandler`。
   - **java8 现状**：`D:\Source\java8\klsjnh-java8-framewor011`，静态注册表 + ThreadLocal 路由，yaml `krt.ci011` 驱动。
   - **java17 现状（本项目）**：`java17-domain011` + `java17-infrastructure011` 已有 877 行动态数据源设施，yaml ci011 驱动、懒加载。
4. 读取老项目 `july_datasource` 建表 DDL、种子 SQL、`JulyDatasourceController`、`JulyDatasourceApplicationService`、`DynamicDataSourceRegistryAdapter`、`JulyDatasourcePoolFactory`、`ExternalDatasourceOps`、`DbType`。

**助手首轮方案要点**

- 定位：`dataservice011` = 数据服务域（连库 → SQL → 建模）。
- 现状缺口四项：数据源来源（yaml vs 表）、管理端点（无 vs 完整）、库类型方言、SQL 管理层。
- 指出现有 `infrastructure011/017.topic-dynamic-datasource.md` 已过时（其"本期无管理端点 / 表驱动二期"表述与现有代码的运行时注册能力不符）。
- 给出 A/B/C 三方案，推荐 A（现有设施原地升级为域）。
- 提出四个待确认点：`juydatasouce` 所指、方案选型、一期范围、老项目参照版本。

**用户回复（原文）**

> 可以写文档
> 本期只做
> dataservice011  july_datasource 表驱动 + CRUD  测试连接	有 testConnection	有 + 临时池（initialSize 0 / maxActive 1 / 10s 超时）不回

**口径确认**

- 方案 A 通过 —— 写文档，本期不动代码。
- **本期范围**：`dataservice011` + `july_datasource` 表驱动 + CRUD + 测试连接（临时池：`initialSize 0` / `maxActive 1` / 10s 超时 / **不回灌注册表**）。
- `july_sql` / `july_model` 与 SQL 执行链路不在本期。

## 会话二：规划 july_dictionary 数据字典（system011）

**用户诉求（原文）**

> 考虑开发下 july_dictionary  july_dictionary_item 还是 items 属于 system011 咱们聊聊

**助手方案要点**

- 归属：`system011`（系统管理域），与 029 配置管理同类；排除 `dataservice011` 与新顶层域；033 建模走跨域读。
- 命名：`july_dictionary` + `july_dictionary_item`（单数，对齐本仓单数惯例；备选 `july_dict_item` / 复数 `items` 不推荐）。
- 模型：主子表；主表、子表都带 `sort_order`（升序）；链接列按 §7 走 `pk_mt`（`MasterLinked`）。
- 程序读入口：`getByType`（下拉/枚举）+ `getLabel`（反查）+ `isValidItem`（合法性校验）；接 030 导出 Provider；每次查库不缓存。

**用户回复（原文，逐条收敛）**

> 1  主子表 2 都带排序

> july_dictionary_item  4 要 5 要

> （问题工具）item 额外展示字段 = 本期只 code/label/sort/status/remark；读缓存 = 本期不加，每次查库

**口径确认**

- 主子表（`july_dictionary` + `july_dictionary_item`），主表与子表都带排序；
- 明细本期只 code/label/sort/status/remark（无 color/css_class/ext_json）；
- 程序读入口 + 反查 + 校验要，导出 Provider 要；
- 每次查库不缓存；编号取 035；仅进入设计阶段，未动代码。

## 会话三：新域 ai011 · july_ai_model_provider（AI 模型接入）

**用户诉求（原文）**

> 新领域 ai011 july_ai_model_provider

> （随后给出 stepfun / longcat / deepseek / asterly 的 主表（provider_code/name/base_url/models）与 子表（api_code/api_name/api_key）样例数据）

> 主子表 带顺序

> 对了带 子表测试链接

> 回显 批准 开吧

> ai011 ai011 注意 swg 组

**口径确认**

- 新顶层域 `ai011`；主子表 `july_ai_model_provider` + `july_ai_model_provider_api`；主表/子表都带 `sort_order`（BasePo011）；
- 子表名 `july_ai_model_provider_api`；`models` 逗号分隔；密钥默认取第一个启用（sort_order）；
- **api_key 出参不回显**（VO 类型层剔除），明文入库、加密二期；
- testConnection 提供商级 + **子表（密钥）级**；
- Swagger 组 `ai011` / displayName `AI模型接入011`；`@Tag` 与表注释一字不差（`AI模型接入011 - 提供商管理`）。

**落地**：DDL `july_ai_model_provider.sql`；域/应用/基础设施/Web 全链路已编码；dev 库建表并写入 4 提供商 + 6 密钥实测（真实 key 不载入本文档）。

## 会话四：存储中心管理面 + 在线编辑 · 迁移方案

**用户诉求（原文）**

> 老项目里有 存储中心和编辑 你找找老代码写方案迁移

**助手调研**

- 老参照：`D:\aikbjava011\java17011\kj17-f016_20260719_a011`（另 `kj17-f011_20260719_a011` 同构）。
- 「编辑」= `domain.storage.model.EditableTextPolicy`：**浏览器内纯文本编辑**（`.sql`→sql 编辑器 / 其余→markdown，≤1MB）。
- 老管理面：`july_storage`（表驱动多实例）+ `StorageResolver`（code→adapter 缓存）+ `ObjectStorageAdapterFactory` + 三个 controller（storage / bucket / object）+ `readText`/`saveText`。
- 本项目现状：只有 `ObjectStoragePort` + `local011`/`minio011` 两适配器（yaml 单实例），**管理面整层未迁**。

**产出（方案态，未动代码）**

- `requirement011/037.topic-storage-migration.md`（业务范围 + 六项待定案）
- `requirement013/037.topic-storage-migration.md`（DDL / 类清单 / 端点 / 改造点 / 路线图 / E2E）
- 更新 `infrastructure011/019` 指向 037；README 台账登记 037（下一可用 038）。

**关键取舍**：表驱动多实例（对齐 031「表为真源」）；provider 用 `StorageType011`；适配器由工厂按实例配置造（改造现状 bean）；`secretKey` 出参不回显；老列名/审计口径不照搬。
