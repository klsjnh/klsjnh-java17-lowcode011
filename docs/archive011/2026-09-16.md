# 2026-09-16 工作日志

## 任务一：037 存储中心管理面 + 在线编辑（迁移并编码）

**状态**：已编码并 E2E（决策六项定案）

- 老参照 `kj17-f016` 管理面迁入：`july_storage` 表驱动多实例。
- 后端：domain（`JulyStorage` / `JulyStorageRepository` / `StorageConnectionConfig` / `StorageResolverPort` / `ObjectStorageFactoryPort` / `EditableTextPolicy`，`ObjectStoragePort` 加 `presignedGetUrl`）；infra（Po/Mapper/Repo + `StorageResolver011` + `ObjectStorageFactory011` + 适配器改造为按配置构造 + `StorageSeed011`/Runner）；application（实例/桶/对象三 UseCase）；web（storage/bucket/object 三控制器 + VOs）。
- 连带修复：`BackupUseCase` 直接注入 `ObjectStoragePort` → 改 `StorageResolverPort`（去单例 bean 后必需）。
- E2E：default 实例播种；testConnection；桶 建/列/查/删；对象 上传/列/stat/readText(`.sql`)/saveText/预签名(local→`file:`)/下载/删；`>1MB` 保存 → 400。

## 任务二：038 低代码核心（一主三子，仅 CRUD）

**状态**：已编码并 E2E

- `july_metadata` + `july_metadata_field/_display/_service`；四表都带 `sort_order`；主表 `object_name` 唯一不可变；三子 `pk_mt`。
- domain 新增 `JulyMetadata` + 三子 record（含 `sortOrder`）；infra 4 PO/4 Mapper/聚合仓储；application UseCase；web 控制器（7 端点）+ Converter + VOs。
- 关键点：主子表**整替**，子行**物理删除**（唯一键 `(pk_mt,code)` 不含 `dr`，逻辑删后重插会撞键——实测 400 duplicate key 后修正）；级联逻辑删除。
- **不改** 033 依赖的 `domain.lowcode011.model` 值对象。
- E2E：insert(主+3子)/getById(按 sortOrder)/getByObjectName/重复名 400/display 绑未知字段 400/update 三子替换/logicDelete 级联(子行清 0)。

## 台账

- README：037 转「已编码」，新增 038；下一可用 **039**。
- 038 E2E 清单见 `requirement013/038.topic-lowcode-core.md` §021。

## 任务四：审计类型枚举 + controller IUD 审计（用户定案）

- **枚举** `common.enums.AuditType011`（LOGIN / LOGIN_FAILED / LOGOUT / CHANGE_PASSWORD / INSERT / UPDATE / DELETE / EXPORT / BACKUP）；`UserAuditPort.record` 由 `String auditType` 改收枚举；`ExportUseCase`/`BackupUseCase`/`JulyUserUseCase` 字面量改枚举。
- **IUD 审计落 controller（service 不碰）**：web 层 `@AuditLog(type, objectCode)` + `AuditLogAspect`（`@AfterReturning`，成功才写；从请求取 `operator`/ip）。web pom 加 `spring-boot-starter-aop`（已离线入库）。
- 覆盖 13 个控制器 IUD 端点（insert/update/logicDelete/logicDeleteBatch/item/api/remove/assign/start·stop/reload/upload·saveText）。
- E2E：配置 insert/update/logicDelete → `july_user_audit` 分别落 INSERT/UPDATE/DELETE（object_code=julyConfig、操作人 test001、IP）；**查询不写审计**。
- 说明：auth 类（登录/改密）与 EXPORT/BACKUP 仍在其动作所在层审计（非 IUD）。

## 任务五：字典 getByCode + 执行 SQL 输入多态

- **字典补 `getByCode`**（GET，含明细）：用例 `getByCodeWithItems`；`req011/035 §019` + `req013/035` 端点表/端点数（10→11）同步。
- **执行 SQL 输入多态**（`executeSql` / `executeSqlByPage` 共用）：
  - 数据源：`dataSourceId` / `dataSourceCode` 至多选一，都不传则由建模推导；
  - SQL 来源：`modelId` / `modelCode` / `sqlContent` 恰好选一（建模取 `sql_content`）；
  - 新增应用记录 `BusinessModelingExecuteCommand`；用例加 `resolve()`（校验 400/404）。
  - E2E 8 组：code+sql、id+sql、modelId、modelCode、modelId+覆盖 ds、无 SQL 源 400、ds id+code 400、model+sql 400 —— 全过。
  - 文档同步 `req013/033`。

## 任务六：容器化部署（Docker Compose，ubuntu 26.04 基镜像）

- **Part 1 基镜像**：`deploy/Dockerfile.base` = `FROM ubuntu:26.04` + JDK17 介质 → `klsjnh/java17:v0.0.1`（已构建验证 java 17.0.12）；`build-base.sh` 介质优先从 MinIO 桶取（参数走环境变量，脱敏）。
- **Part 2 依赖两方案**：**mount（默认，轻量挂载）** `/deploy/docker-compose.yml`；**bake（拷贝进镜像）** `deploy/docker-compose.bake.yml` + `Dockerfile.project`。
- **编排在项目内**（不用 `/klsjnh/docker`）；`network_mode: host` 连宿主 MySQL；`entrypoint` 用 `$$JAVA_OPTS` 防 compose 插值。
- **实测**：`docker compose -f deploy/docker-compose.yml up -d` → 容器 Up、`http://127.0.0.1:23333/doc.html` 200、日志 `deploy/runtime/logs/java17-app011.log`。
- **脱敏**：`deploy/` 无内网 IP/凭据；`deploy/runtime/` gitignore；敏感配置部署机自建。
- **归档**：`ubuntu_26.04.tar`、`klsjnh_java17_v0.0.1.tar` 已 `docker save` 上传 MinIO `docker-images`。
- 文档：新增 `docs/infrastructure011/020.topic-deploy-docker.md`。

### 部署编排对齐服务器既有 `compose.yaml` + 验证

- 约定对齐：`networks: [service011]`（bridge）+ `ports: 23333:11610` + `logging`(json-file 100m×3) + `healthcheck`(`/dev/tcp/11610`)；追加到 `/klsjnh/docker/compose.yaml`（`./java011/<项目>/...` 相对）。
- 路径约定：**容器内可挂载目录一律在 `/klsjnh/volume` 下**（config/logs/data/storage011）；`storage011` 由 `/klsjnh/app` 迁至 `/klsjnh/volume`；deploy.sh 自动把 deploy 副本的 `base-path` 指过去。
- 修：bake compose 的 `${APP_IMAGE:?...}` 未加引号 → YAML 报错（已加引号）。
- 验证：两份 compose `config` 通过；`bash -n` 通过；本机 mount 冒烟 `Up(healthy)` + `23333→doc.html 200` + 日志落盘（DB 走桥接网关 `172.17.0.1:33306` 代 `mysql013`）。
- `.gitignore` 增 `deploy/java011/`。

---

## 任务三：工程卫生整改（用户授权）

### 1. `.backup011/` 去跟踪
- `git rm -r --cached .backup011`（**205 个文件移出索引，本地文件保留**）；`.gitignore` 加 `.backup011/`。
- 未提交（等用户 commit 指令）。

### 2. 逻辑删除 + 唯一键 根治
- 现状：dr 逻辑删除下 `UNIQUE(业务列)` 让墓碑挡住同键重插（重复插入 500），仅靠 `DuplicateKeyException→400` 兜底。
- 根治：唯一索引改建在**生成列 `alive_*`**（`dr='0'` 时=业务键，否则 `NULL`）——只约束存活行，墓碑保留；MySQL 多 NULL 不冲突。
- 覆盖 **19 个唯一键 / 15 张表**；迁移 `docs/sql/logic-delete-unique-fix.sql`（已执行到 dev 库）；规则写入 `docs/sql/base-entity-columns.sql`；`docs/019` 缺口 #2 闭环。
- E2E：`july_config`（单键）与 `july_dictionary_item`（复合键）删除后重插 → 200；存活行重复 → 400。
- 无代码改动（纯 DB/DDL），`DuplicateKeyException→400` 保留为最后兜底。
