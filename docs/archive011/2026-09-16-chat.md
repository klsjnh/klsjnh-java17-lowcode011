# 2026-09-16 原始对话记录

> 脱敏说明：本文不含凭据 / 内网 IP / 密钥 / token。

## 会话一：存储中心管理面迁移（037）

**用户诉求（原文）**

> 老项目里有 存储中心和编辑 你找找老代码写方案迁移
> …（期间插入）等下 要开发低代码的核心模块 一主三子 能懂不
> 先说清楚这个事情
> 1 一主三字 都带 排序 2 先就落 curd 别的不管 你自己测试好
> 好了 你干吧 我要睡觉了

**助手调研**

- 老参照 `kj17-f016_20260719_a011` 存储中心：`july_storage` 表驱动多实例 + `StorageResolver` + `ObjectStorageAdapterFactory` + storage/bucket/object 三控制器 + 在线编辑 `EditableTextPolicy`（`.sql`→sql / 其余→markdown，≤1MB）。
- 「编辑」= `readText`/`saveText` 浏览器内纯文本编辑。
- 本项目现状：只有 `ObjectStoragePort` + local011/minio011 两适配器（yaml 单实例），管理面整层未迁。
- 决策六项：表驱动多实例 + yaml 播种 / provider 用 `StorageType011` / 带 sort_order / 预签名本期做 / 端口维持 byte[] / 编辑 sql·markdown+1MB。

## 会话二：低代码核心（038，一主三子）

**用户诉求**：低代码核心模块，「一主三子」+ 都带排序 + 仅 CRUD。

**助手理解**：主 `july_metadata` + 三子 `july_metadata_field/_display/_service`；为不破坏 033 依赖的 `domain.lowcode011.model` 值对象，另建持久化就绪聚合 `JulyMetadata` + 三子 record。

**表名定案**：`july_metadata` / `july_metadata_field|display|service`（全称、无 011 后缀、子名词不加 info/data）。

## 落地（助手自测）

- 037：`/klsjnh/storagecenter/{storage,bucket,object}`；`BackupUseCase` 连带改 `StorageResolverPort`。
- 038：`/klsjnh/lowcode011/julyMetadata/v1`（7 端点）；主子整替（子行物理删）+ 级联逻辑删除。
- 详见 `2026-09-16.md` 与两份 requirement013 技术方案。
