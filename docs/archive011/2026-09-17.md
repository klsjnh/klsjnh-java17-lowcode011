# 2026-09-17 工作日志

## 任务一：037 存储中心包名与路由收敛（storagecenter，三控制器→两模块）

**状态**：已编码并 E2E（用户授权，自主完成）

- **包名收敛**：`domain/infrastructure/application/web` 各层 `storage011` / `storage` → `storagecenter`（47 文件 + 4 目录，类名不变）。
- **路由定案**：`/klsjnh/storagecenter/julyStorage/v1`（实例 + 桶）、`/klsjnh/storagecenter/julyObject/v1`（对象）；对齐「域全小写 + 模块小驼峰」约定。
- **控制器收敛**：三控制器 → 两控制器
  - `JulyStorageController` 并入桶动作（注入 `StorageBucketUseCase`）；
  - `StorageObjectController` 更名 `JulyObjectController`；
  - 删除 `StorageBucketController` / `StorageObjectController`。
- **动作更名**：
  - 实例 `insert` / `update` / `logicDelete` / `logicDeleteBatch` / `getById` / `getByCode` / `selectListByPage` / `testConnection`；
  - 桶 `selectBucketList` / `selectBucketListByPage` / `getBucket` / `insertBucket` / `removeBucket` / `testBucketConnection`；
  - 对象 `selectObjectList` / `selectObjectListByPage` / `statObject` / `uploadObject` / `downloadObject` / `presignObjectUrl` / `removeObject` / `batchRemoveObject` / `readObjectText` / `saveObjectText`。
- **校验**：门禁 0 违规 + `mvn -o clean compile` 通过 + `./script011.sh dev013` 启动成功。
- **E2E**：
  - julyStorage 9 组（实例 增/改/逻辑删/点查/分页/探活 + 桶 建/查/列/探活/删）全 200；
  - julyObject 11 组（上传/列/分页/stat/readText/saveText/预签名/下载/单删/批删）全 200，`local011` 预签名 → `file:` URI；
  - 旧路由 `/klsjnh/storagecenter/{storage,bucket,object}` → **404**（迁移干净）；
  - 其它模块回归 `system011` / `dataservice011` / `ai011` / `lowcode011` 分页均 200；
  - Swagger 组不变，端点数 julyStorage 14 / julyObject 10。
- **文档同步**：`requirement011/013 037`、根 `README.md`、`docs/README.md` 全部改为新前缀与两模块；09-16 日志归档至 `archive011/`。

## 任务二：对象分页接口补元数据（对齐老代码 `listObjects`）

**状态**：已修复并 E2E

- **问题**：`julyObject/v1/selectObjectListByPage` 返回 `List<String>`（纯键），老 `kj17-f016` 的 `StorageHandle.listObjects` 返回 `Map{bucketName,objectName,size,etag,lastModified}`，前端文件列表因此拿不到名称/大小/时间。
- **修复**：`ObjectStoragePort` 新增 `listStat(bucket,prefix)`（一次遍历/一次 list 拿元数据，避免逐行 stat）；`LocalObjectStorageAdapter` 走 `Files.walk`（size/lastModified/`probeContentType`），`MinioObjectStorageAdapter` 走 `listObjects` 的 `Item`（size/lastModified，contentType 留空）；`StorageObjectUseCase.selectListByPage` 改回 `PageResult011<ObjectStat>`，控制器同步。`selectObjectList` 保持键字符串数组。
- **E2E**：local011 行 `{key,size:15,lastModified,contentType:"application/sql"}`；minio011（yw011/docker-images）18 项分页正常、size/lastModified 正确。
- 约定：分页包裹仍用新项目 `PageResult011`（pageIndex/pageSize/rows），只把行结构补回元数据。

## 任务三：桶与实例出参对齐老 f016

**状态**：已修复并 E2E（用户定：桶列表、getBucket、实例全部对齐老代码）

- **桶列表**：`ObjectStoragePort.listBuckets` 由 `List<String>` 改为 `List<BucketInfo>{bucketName,creationDate}`（domain 新增 `BucketInfo`）；local 取目录 `creationTime`，MinIO 取 `bucket.creationDate`。`selectBucketList` / `selectBucketListByPage` 同步。
- **getBucket**：由 `Boolean` 改为 `{bucketName,exists}`（application 新增 `StorageBucketExists`）；桶不存在抛 404，对齐老 `getByName`。
- **实例出参**（`JulyStorageVo011` + `JulyStorageConverter`）：字段更名 `createBy/updateBy/createTime/updateTime` → `createdBy/updatedBy/createDate/modifyDate`；`secure` 由 boolean 改 `"1"/"0"`；`accessKey`/`secretKey` 均打码 `******`（含新增 secretKey 出参）。
- **E2E**：桶列表（local `{demo011,...}`、MinIO yw011 12 桶）带 creationDate；getBucket 命中 `{bucketName,exists:true}`、缺失 404；实例 getByCode 回 `accessKey/secretKey:"******"`、`secure:"0"`、`createdBy/.../createDate/modifyDate`。
- **testConnection 补齐**（见任务四）：`{success,bucketCount,basePath|endpoint,message}`。

## 任务四：testConnection 补齐 bucketCount + basePath|endpoint

**状态**：已修复并 E2E

- 新增 domain `StorageProbe{success,bucketCount,basePath,endpoint,message}`（工厂 `local(...)` 只带 basePath、`remote(...)` 只带 endpoint）。
- `ObjectStoragePort` 新增 `testConnection()`，由适配器自省：local 校验 basePath（存在/目录/可写）并数桶目录；MinIO 列桶并回 endpoint。失败在载荷回报（恒 200），不抛。
- `JulyStorageUseCase`（已保存/草稿）与 `StorageBucketUseCase` 改用端口探针；删除 `StorageProbeResult`。
- 新增 web `StorageProbeVo011`（`@JsonInclude(NON_NULL)`），控制器映射，保证老样本形状：local 只出 `basePath`、S3 系只出 `endpoint`。
- **E2E**：local `{success:true,bucketCount:2,basePath:"./storage011",message:"ok"}`；yw011 `{...bucketCount:12,endpoint:"http://192.168.1.88:11230"...}`；坏路径 `{success:false,bucketCount:0,basePath:"/no/such/dir",message:"base path does not exist ..."}`。

## 任务五：其余老口径差异（保留新项目口径，用户未要求改）

- 仍按新项目包裹：`Response011` + `PageResult011{rows,pageIndex,...}`；`insert/update` 回 `IdVo011`（老只回 message）；实例分页过滤字段沿用 `keyword`（老用 storageCode/storageName/provider/status）。

## 任务六：默认桶改为适配器配置声明（backup011 落 base-path/default-bucket）

**状态**：已实现并 E2E（用户定：只改配置文件，不动播种）

- **配置**：`krt.storage-center.local011.default-bucket: backup011`（`StorageProperties.Local011` 增字段，默认 `backup011`）；`minio011.default-bucket` 保持。物理位置 = `base-path/default-bucket`。
- **读取**：新增 domain `StorageDefaultsPort`（active adapter 默认桶）+ infra `StorageDefaults011`（按 `default-type` 取 local011 / minio011 的 default-bucket，空回退顶层 `default-bucket`）。
- **备份**：`BackupUseCase` 注入该端口，桶 = 配置默认桶，空则回退实例行 `july_storage.default_bucket`。
- **E2E**：`backup011` → `./storage011/backup011/julyConfig/20260917_162748.json`；`selectBucketList` 出现 `backup011`。
- 播种未动（`StorageSeed011` 仍按原逻辑），表仍是实例/连接的运行时权威；本项只把「默认桶」提到配置。

## 任务七：039 低代码设计器与运行时（立案，分三期）

**状态**：立案完成，一期开工（用户批准）

- **背景**：`MetadataDesigner` 在旧 java8 runtime013（`business/metadata011`）与 aikb java17-runtime013（`business/dataservice011`）都有；pc 前端 `services/lowcode011/metadataDesignerService.ts` 已预埋 10 个 PENDING-BACKEND 能力。新平台只有 038 的元数据 CRUD。
- **范围（全做）**：设计 → 探测（复用 dataservice011）→ 发布建表（DDL 执行）→ 数据初始化/同步 → 运行时（动态 CRUD + 运行页）→ 开放 API。
- **三条核心决策**：
  1. **发布快照 + 编辑态分离**：新增 `july_metadata_version`（MetaDTO 快照 + ddl + 物理/备份表）；`july_metadata` 是 DRAFT 态。
  2. **DDL 边界**：首次 `CREATE TABLE IF NOT EXISTS`，其后**仅 `ALTER`**（ADD COLUMN/INDEX/UNIQUE），**禁 `DROP`/`MODIFY`/`RENAME`**；开关默认 false；表 `lc_` 前缀 + 独立库隔离。
  3. **运行时权限模型单列**（对象级 + 操作级）。
- **三期**：一期 设计闭环（只读，含 `previewDdl`）；二期 发布闭环（写库）；三期 运行时 + 开放 API。
- **文档**：新增 `requirement011/013 039`；台账 `docs/README`（下一可用 **040**）+ 根 `README` 状态表更新。
- **端点前缀**：`/klsjnh/lowcode011/julyMetadata/v1`（对齐前端 BASE `/klsjnh/lowcode011` + `actions.ts`）。

## 任务八：039 一期（设计闭环·只读）落地并 E2E

**状态**：一期完成（`bpm_user011` 跑通）

- **DDL**：`docs/sql/july_metadata_publish.sql`（`july_metadata` 加发布态列 + `july_metadata_version` + `july_metadata_open_api`），已执行到 dev。
- **domain**：`JulyMetadataVersion` + `JulyMetadataVersionRepository` + `MetadataDdlGeneratorPort`。
- **infra**：`JulyMetadataVersionPo/ Mapper/RepositoryImpl`（不继承 BaseRepository，append-only by object_name+version）+ `MySqlMetadataDdlGenerator`（标识符强校验、类型映射、表 `lc_` 前缀）。
- **application**：`JulyMetadataDesignerUseCase`（listModels/load/save/previewDdl；一主三子 ↔ MetaDTO 组装内聚）。
- **web**：`JulyMetadataDesignerController`（挂 `/klsjnh/lowcode011/julyMetadata/v1`）。
- **E2E**：`save`(10 字段/3 显示/4 服务) → `load` → `listModels` → `previewDdl`（`CREATE TABLE IF NOT EXISTS lc_bpm_user011(...)`）。
- **探针遗留**：033 probe 带 `KLSJNH_RN` 伪列、不回别名；039 保存前手工剔除，建议修 033。
- **下一步**：二期（发布闭环·写库：`publish/syncData/importDataFromSql/importStatus` + DDL 执行开关/幂等）。

## 任务九：039 二期·发布建表（写库）落地并 E2E

**状态**：publish 完成；数据同步（import/sync）进行中

- **domain**：`MetadataDdlGeneratorPort` 增 `generateAddColumns`；新增 `MetadataDdlExecutorPort`（tableExists/columnsOf/execute）；`JulyMetadataRepository` 增 `updatePublishState`。
- **infra**：`MySqlMetadataDdlGenerator.generateAddColumns`（仅 ADD COLUMN）；`DatasourceDdlExecutor`（主数据源 JdbcTemplate + `krt.lowcode.ddl-execute.enabled` 开关，默认 false）；`JulyMetadataRepositoryImpl.updatePublishState`。
- **application**：`JulyMetadataPublishUseCase.publish`（首建 CREATE / 后仅补缺列 / 写快照 / 推进发布指针 / 版本 0.0.1 递增）。
- **web**：`JulyMetadataPublishController` → `POST /publish`。
- **E2E**：publish#1 建 `lc_bpm_user011`(0.0.1) → publish#2 幂等(0.0.2, ddl=null) → 加 email 列 publish#3 `ALTER ADD email`(0.0.3)；表/指针/快照三处一致。
- **修正**：新表补 `sort_order`（`BasePo011` 需要），DDL 文件同步。
- **待做**：`importDataFromSql/importStatus/syncData`（源 bpm011 Oracle 分页读 → 目标 `lc_bpm_user011` 写）。

## 任务十：039 二期·数据分页同步落地并 E2E

**状态**：二期完成（发布 + 数据同步）

- **业务键**：`sid` 为业务唯一主键；设计器 `save` 归一 `sid` 为 `VARCHAR(33)`；生成器兜底；`CREATE` 附 `UNIQUE KEY uk_<table>_sid`。
- **domain**：`MetadataDataWriterPort`；`MetadataDdlGeneratorPort.generateCreate(..., businessField)`；`JulyMetadataRepository.markSynced/isSynced`。
- **infra**：`JdbcTemplateMetadataDataWriter`（源→目标列名大小写无关匹配、缺基列补默认、按业务键 `ON DUPLICATE KEY UPDATE`）；`MySqlMetadataDdlGenerator` 业务键强制 `VARCHAR(33)` + 唯一键。
- **application**：`JulyMetadataDataSyncUseCase`（`importDataFromSql` 复用 033 `executeSqlByPage` 分页读 → writer upsert；`importStatus`）。
- **web**：`JulyMetadataDataSyncController` → `importDataFromSql` / `importStatus`。
- **E2E**：page1 100 行；重导 page1 仍 100（**幂等**）；page2 → 200；再导 page1 仍 200；`mode` init→sync。
- **简化**：`updated/unchanged` 以 processed 近似；`syncData` 用分页循环代替。

## 任务十一：业务字段映射平台基列（B 口径）验证并入档

**状态**：验证通过，已入 `req011/013 039`

- **场景**：tuser 建模加 `status` 并映射系统 `status`。
- **B 口径**：导入 SQL 带该列 → 同步按列名大小写不敏感写入平台基列；**元数据/代码零改动**。
- **禁**：元数据再加同名列（`uk_pk_mt_field_code` 大小写不敏感 → 400）。
- **代价**：平台 `status` 启用/停用语义被业务值占用（admin 启停与同步互相覆盖）；`dr` 不受影响。
- **实测**：`bpm011.tuser` 4579 行 → `lc_bpm_user011.status` 1:1822 / 2:2757，与源一致。

## 任务十二：外键/引用列约定（`pk_xxx`）入档

**状态**：验证并入档 `req011/013 039`

- **约定**：`pk_<实体>`（如 `pk_organization`），固定 `VARCHAR(33)`（对齐 `id`）。
- **路径**：A 路径 —— 设计器加字段（string/33）→ `publish` 自动 `ALTER ADD COLUMN` → 导入别名映射（`orgid pk_organization`）；无代码改动。
- **实测**：`bpm011.tuser` 4579 行，源 `orgid` 非空 4566 → `lc_bpm_user011.pk_organization` 非空 4566，与源一致；版本 0.0.2。
- **文档**：`req011/039 §018.1` + 决策记录第 10 条；`req013/039 §013` 补 A 路径机制。

## 任务十三：039 三期·开放 API（api_key 公共 CRUD + meta）落地并 E2E

**状态**：开放 API 完成（运行时的动态 CRUD 已由开放 API 覆盖大部分；菜单/运行页待做）

- **domain**：`JulyMetadataOpenApi` + 仓储；`MetadataDataAccessPort`（select/count/insert/updateByKey/deleteByKey）。
- **infra**：`JulyMetadataOpenApiPo/Mapper/RepositoryImpl`；`JdbcTemplateMetadataDataAccess`（标识符校验 + 全绑定 + `dr` 逻辑删）。
- **application**：`JulyMetadataOpenApiUseCase`（读/存/轮换，apiKey 32hex、打码、明文一次）；`OpenApiUseCase`（meta/query/insert/update/delete，鉴权 + 白名单列 + 补基列默认）。
- **web**：`JulyMetadataOpenApiController`（admin，JWT）+ `OpenApiController`（`/klsjnh/open/v1`，`X-Api-Key`）；`GlobalAuthFilter` 白名单加 `/klsjnh/open/`。
- **E2E**：配置前 closed → 保存 apiKey（回明文）→ 无 JWT `meta`（MetaDTO 11 字段）/`query`(4579) → `insert`/`update`(affected 1) → `delete`(逻辑删 dr=1，查询 total 0) → 错误 key 401。
- **规约**：禁 path variable（objectName 走 query/body）；仅已发布对象；表/列以库表目录为白名单。
- **待做**：`publishMenu/listRuntimeMenus/initialize` + `/runtime/<object>` 运行页；运行时权限模型；开放 API 限流。

## 任务十四：040 元数据模板导入导出（立案，待评审）

- **背景**：给低代码一个**声明式 JSON 模板**入口（AI/外部工具）：导出 / 空白骨架 / 导入 + dryRun 预演 + 可选发布。
- **形态**：`MetaDTO + source + templateVersion`；**不新增表**，复用 038/039。
- **端点**：`GET /template/blank`、`GET /template/export?objectName=`、`POST /template/import`。
- **校验**：保留基列不可作业务字段、子 code 大小写不敏感唯一、`businessField`/`pk_*` 固定 `VARCHAR(33)`、枚举、display 绑定；`dryRun` 零副作用。
- **文档**：`requirement011/013 040`；台账 040 立案，下一可用 **041**。
- **状态**：方案已落，等用户评审后再开工。

## 任务十五：040 元数据模板（按用户端点命名）实现并 E2E

**状态**：完成

- **端点**（用户定）：`GET /getTemplate011`（默认骨架，`_guide` 代注释）、`GET /downloadTemplate011?objectName=`（导出 1 主 3 子 + source）、`POST /uploadTemplate011`（**multipart 单文件**或 JSON，审核零副作用）、`POST /deployObject`（`{template|objectName, overwrite?}` → 落库 + 发布）。
- **实现**：domain `JulyMetadataSource` + 仓储 `updateSource/findSource`；infra `JulyMetadataPo` 加 source 列；app `JulyMetadataTemplateUseCase`（blank/export/review/deploy + 校验归一）；web `JulyMetadataTemplateController`（4 端点、双入口 consumes）。
- **关键修正**：`load/export` 的 `fieldData` **本就含平台基列**，原「保留字拒绝」会误杀导出→上传往返；改为**基列允许一次 + 类型归一**，重名（含大小写）才算冲突。
- **E2E**：往返 `valid/plan=alter`；新对象 `bpm_user_tpl` `plan=create`→`deployObject` 建 `lc_bpm_user_tpl`(0.0.1，含唯一键，`data_source_code` 持久化)；追加业务 `status` → `duplicate field code`；multipart 通过；`objectName` 直发 / `overwrite=false` 400 / `overwrite=true` 更新；导出带出 `source`。
- **文档**：`requirement011/013 040` 更新为最终形态（含 §020 E2E、§023 决策 7 条）。

## 任务十六：前端对接指南 + AI 低代码机制两份文档

- `docs/infrastructure011/021.topic-lowcode011-frontend-guide.md`：接入基线（信封/分页/时间/禁 path variable）、低代码 5 组端点、MetaDTO 形状与枚举、开放 API、存储中心、**前端血泪清单**、建议页面。
- `docs/infrastructure011/022.topic-ai-lowcode-pipeline.md`：端到端流水线（探源→模板→审核→部署→导数据→开放 API）、能力矩阵（已做/挂起）、**端点即 AI 工具**、标准 AI 流程、安全闸门、约束与坑、`bpm_user011` 全流程示例、未来（运行时/真映射/MCP/沙箱）。

## 任务十七：039 三期·运行时段（菜单 + 运行时数据）落地并 E2E

**状态**：运行时段（后端）完成

- **application**：`JulyMetadataRuntimeUseCase`（publishMenu / listRuntimeMenus / meta / query / insert / update / delete）。
- **web**：`JulyMetadataRuntimeMenuController`（`/julyMetadata/v1` 的 `publishMenu`/`listRuntimeMenus`）+ `JulyMetadataRuntimeController`（`/klsjnh/lowcode011/runtime/v1` 的 meta/CRUD，JWT）。
- **菜单落点**：`july_menu` 建 `rt_<object>`（menuType=2，route `/runtime/<object>`），幂等（重复发布更新）。
- **E2E**：publishMenu → `july_menu` 出现 `rt_bpm_user011`；listRuntimeMenus 返回（version 0.0.3/published）；runtime meta(11 字段)/query(4579)/insert/update/delete（逻辑删）全过。
- **简化**：`initialize` 不适用（表由 `docs/sql` 迁移管理）；运行页（前端）另行；按元数据 required/类型校验 + 动态审计 + 权限模型仍挂起。

## 任务十八：运行时按元数据校验（required/类型/长度）

- **新增**：`MetadataValueValidator`（application）——insert 校验必填 + 类型（int/float/boolean/date/string 长度），update 仅校验出现字段；基列（create_by/update_by/create_time/update_time）不强制必填。
- **接入**：`JulyMetadataRuntimeUseCase.create/update`（先补基列默认再校验）。
- **E2E**：缺 `sid` → 400；`sid` 超长(>33) → 400；合法 insert → affected 1；局部 update（只改 name）→ 通过；超长 name → 400；清理逻辑删。
- **仍挂起**：动态 `objectCode` 审计、运行时权限模型。

## 任务十九：新建 `docs/lowcode011/` 知识库（metadata011 体系）

- 新建目录 `docs/lowcode011/`，落 `011.topic-metadata011-system.md`：一句话定位 / 老→新来龙去脉 / 数据模型（一主三子 + 旁路表）/ MetaDTO 形状 / 枚举体系 / 生命周期 / 全链路 / 硬约定 / 与其它模块关系 / 现状取舍 / 代码索引。
- `docs/README` 专题表加 `lowcode011/` 行；requirement011/013 描述补 037/039/040。

## 任务二十：lowcode011 知识库补 021~024（值对象体系）

- `docs/lowcode011/013.topic-metadata011.md`：MetaData011 聚合根（组成/不变式/工厂/MetaDTO 映射）。
- `docs/lowcode011/015.topic-fieldinfo.md`：FieldInfo011（字段，FieldType011 映射、硬约定）。
- `docs/lowcode011/016.topic-displayinfo.md`：DisplayInfo011（展示列，DisplayType011、绑字段）。
- `docs/lowcode011/017.topic-serviceinfo.md`：ServiceInfo011（服务，ServiceObjectType011/ServiceParamType011）。
- `docs/README` lowcode011 行补 021~024。

## 任务二十一：lowcode011 知识库细分拆开（025~033）+ 字典字段草稿

- **细分主题**（把 038/039/040 拆开讲）：`025` JulyMetadata（持久化聚合/一主三子/整替）· `026` MetaDTO（契约与映射）· `027` 设计器 · `028` 发布与 DDL · `029` 数据同步 · `030` 运行时 · `031` 开放 API · `032` 元数据模板 · `033` 字典字段。
- **字典字段草稿**（核心架构变更）：`FieldType011` 增 **`dictionary011`/`dictionary013`**（全称不缩写）；`FieldInfo011` 增 `param011`；`dictionary011` 选项内联 `1|是;2|否`（值/标签禁含 `|` `;`）；`dictionary013` 存字典编码，复用 `JulyDictionaryUseCase`（依赖形态 A，校验软硬暂缓）；物理列 `VARCHAR(length)`；`022` 增「字典字段」节、`023` 增渲染约定。
- `docs/README` lowcode011 行列全 011·021~033。

## 任务二十二：低代码文档并入 `docs/lowcode011/`（编号 011/013/015/016…）

- **迁入**：`requirement011/{038,039,040}` → `lowcode011/`（业务规则）；`requirement011/` 不再含低代码主题。
- **重排编号**（用户口径 `011/013/015/016…`）：`011 总览 · 013 MetaData011 · 015 FieldInfo011 · 016 DisplayInfo011 · 017 ServiceInfo011 · 018 JulyMetadata · 019 MetaDTO · 020 设计器 · 021 发布与DDL · 022 数据同步 · 023 运行时 · 024 开放API · 025 模板 · 026 字典字段 · 027 低代码核心（业务）· 028 设计器与运行时（业务）· 029 元数据模板（业务）`。
- **引用更新**：全仓 18 处链接修正；`docs/README` 专题表 + 台账 038/039/040 行、requirement011 描述改为指向 `lowcode011/`。
- 门禁 PASS、0 坏链。（`requirement013` 技术方案按用户口径暂不动。）

## 任务二十三：ai011 AI 调用能力立案（041，待评审）

- **需求**：平台内要能"直接调 AI"（现只有配置 + testConnection）；`provider` 与 `api` **均支持 id 或 code**；示例 `longcat`；供 **AI 自开发** 与内部服务。
- **方案**：domain `AiChatPort`（+ `ChatCommand/Message/Result`）· infra `OpenAiCompatChatAdapter`（`POST {baseUrl}/chat/completions`，Bearer，超时）· app `AiInvokeUseCase`（id/code 解析 + 默认密钥 + 脱敏 + 审计）· web `POST /klsjnh/ai011/julyAiChat/v1/chat`。
- **边界**：首版非流式 chat；不做流式/embedding/多模态/会话存储/限流计费。
- **文档**：`requirement011/013 041`；台账 041 立案，下一可用 **042**。
- **状态**：方案已落，等用户评审（代码未动）。

## 任务二十四：AI 调用（041）实现并自测 + 架构评审记录（lowcode011/030）+ 多入口建模提案（042）

- **041 实现**（service + controller 双能力）：domain `AiChatPort`(+Command/Message/Result)、infra `OpenAiCompatChatAdapter`、app `AiInvokeUseCase`(+Outcome)、web `AiChatController` + VOs，端点 `/klsjnh/ai011/julyAiChat/v1/chat`。
- **041 E2E**：longcat 按 **code** 与按 **id** 均 200（回内容 + usage）。
- **本轮 1/2/4/5 复验**（同次构建）：探针不再带 `KLSJNH_RN`（字段 `id/SID/USERNAME/NAME/...`）、同步缺业务键 → 400、运行时审计 `object_code=bpm_user011`、物理表策略入档。
- **架构评审记录**：`docs/lowcode011/030.topic-engine-refactor.md`（组合契约 / context map / 内核抽取 / 边界定型 / 横切端口化；含对方案的 4 点纠偏）。
- **043 提案**：`requirement011/013 043`（多入口建模与数据同步：一个引擎、N 个门）。**注**：`042` 已被「内核重构」（`requirement013/042.topic-lowcode-kernel-refactor.md`）占用，故多入口建模用 043。
- **SSR 契约**：前端 SSR 按后端 `meta`(MetaDTO) 动态渲染表单/列表（见 `lowcode011/023` §018.1）。
- 台账：041/042/043 立案，下一可用 **044**。

## 任务二十五：契约 S1（MetadataContent + 单一映射）落地

- **裁定（用户）**：不搞大爆炸 —— **先写契约、不下沉、慢慢过渡**（Strangler）。两条护栏：新代码契约优先；映射只在 `MetadataContentMapper` 一处。
- **S1 完成（零行为改动）**：
  - `domain.lowcode011.MetadataContent`（record，不可变；JSON 键仍同 MetaDTO）+ `reconstitute`（边界卫兵）。
  - `domain.lowcode011.MetadataContentMapper`（**单一**映射）：`from(JulyMetadata)`、`from(MetaData011)`。
- **未下沉**：不碰 033、不解除 038、不改任何现有端点/行为；子类型暂用引擎 record（含 `sortOrder`，保真；`model/` 族 S4 再 record 化）。
- **文档**：`requirement013/042` 增「过渡路径 S1–S4」，S1 标完成。
- **校验**：门禁 PASS、编译 BUILD SUCCESS、0 坏链。

## 任务二十六：042 期二（内核抽取）+ 期四（安全清理，3/4）

- **期二 ✅**：`domain.ObjectTablePolicy`（纯策略：filter/基列默认/logicDelete/keyColumn/page clamp [1,500]/dr 过滤）+ `application.ObjectTableGateway`（physicalTable/query/insert/update/delete，注入 `MetadataDataAccessPort`+校验器+版本/元数据仓储）；`JulyMetadataRuntimeUseCase`、`OpenApiUseCase` 改走网关并删私有复制（`fillBaseDefaults/filter/physicalTable/integer/ensureValid`）。
- **期四（裁剪版）部分 ✅**：`LocalDateTime.now()` 5 处归口 `DateUtil011`；`@Value` 清零（`DatasourceDdlExecutor`、`OpenAiCompatChatAdapter` → `KrtConfig011` 新增 `lowcode`/`ai011` 段）；`OpenApiUseCase` apiKey **NPE→401**。**`CurrentOperatorPort` 未做**。
- **常量**：`domain.lowcode011.records.BaseColumn011`（id/status/dr/create_by/update_by/create_time/update_time + ALL/AUDIT）、`BusinessKey011.FIELD="sid"`；`ObjectTablePolicy`、`MetadataValueValidator`、`JulyMetadataTemplateUseCase` 改用常量。
- **契约包**：`MetadataContent`/`MetadataContentMapper` 移入 `domain.lowcode011.records`（`record` 是上下文关键字，用 **`records`**）。
- **E2E（bpm_user011）**：运行时 query(4579)/CRUD + 审计、开放 API query/insert/delete + 无 key **401**、AI chat(longcat)、模板导出→上传 `valid/plan=alter`，全过。
- **未做**：S2/S3/S4、期三（`ObjectQueryCommand/RowView`、`meta`→`getMeta`）、期一"下沉"（`JulyMetadata` 组合契约 + 033 联动）、`CurrentOperatorPort`。

## 任务二十七：042 期三（getMeta）+ 期四（CurrentOperatorPort）收尾 + 清理

- **期三**：`meta` GET → **`getMeta`**（运行时 + 开放 API，三层同步）；`ObjectQueryCommand/RowView` 未做。
- **期四 ✅**：`CurrentOperatorPort`（`common.OperatorContext011` ThreadLocal + `GlobalAuthFilter` 填充/清理 + `infra.CurrentOperatorAdapter` + 网关落 `create_by/update_by`）；运行时不再逐方法传 `Operator011`（审计从端口取）。
- **E2E**：`runtime/getMeta`、`open/getMeta` 200；运行时 insert → `create_by/update_by = 操作者 id`（实测）。
- **清理**：删除 `tmp/`（gitignore 的一次性脚本/编译碎片）；`.backup011`（用户备份）保留。
- **仍挂起**：S2/S3/S4、期三 `ObjectQueryCommand/RowView`、期一"下沉"（`JulyMetadata` 组合契约 + 033 联动 + 单一 JSON codec）。

## 任务二十八：042 期一（组合部分）—— JulyMetadata 组合 MetadataContent

- `JulyMetadata` 由"自持三套子实体"改为**组合 `records.MetadataContent`**：`content` 是 `fields/displays/services` 的**单一来源**；`create/update/replaceChildren` 重建 content（标量留在聚合）；`fields()/displays()/services()` 委托 `content`，新增 `content()` 访问器。
- **不动 033、不删引擎 record**（那属破坏性跨域部分）。
- **E2E**：`getByObjectName`（11/3/4）、`load`（11）、模板导出→上传 `valid/alter`、runtime query(4579) + insert/delete（含必填校验 400）、`deployObject`(0.0.5)、AI chat，全过。
- **未做**：删 `JulyMetadataField/Display/Service`、`model/` 族 record 化、033 联动、单一 JSON codec、S2/S3/S4、期三 `RowView/ObjectQueryCommand`。

## 任务二十九：单一 JSON codec + 常量收敛

- **`records.MetadataContentCodec`**：`MetadataContent ↔ MetaDTO` 的**唯一**编解码（`fromMetaDto` 走 `reconstitute` 校验）；`JulyMetadataDesignerUseCase.load` 改走 codec（输出不变：remark/sortOrder/publishStatus/version 由 designer 合并）。
- **`records.MetaDtoKey011`**：MetaDTO JSON 键常量；codec + designer.load 全部改用常量。
- **`JdbcTemplateMetadataDataWriter`**：`resolveColumns`/`defaultValue`/`#default:` 前缀改用 `BaseColumn011` 常量。
- **E2E**：load(11 字段 + remark/sortOrder)、getByObjectName、模板导出→上传 `valid/alter`、runtime query(4579)、import page1（sync/processed 50）全过。
- **未做**：删引擎 record、`model/` 族 record 化、033 联动、S2/S3/S4、期三 `RowView/ObjectQueryCommand`。

## 任务三十：042 期一收尾——033 收敛契约 + 删 model/ 旧族（破坏性）

- **033 收敛**：`JulyBusinessModeling` 改持 `MetadataContent`（`metaData()`→`content()`，add/update/remove/replaceFields 走 copy-on-write）；`ModelDataCodec011` 编解码契约（`model_data` JSON 键不变）；`FieldInferencePort` / `FieldTypeInferrer011` / `BusinessModelingProbeResult` / `JulyBusinessModelingUseCase` / web converter 全部改用契约 record（`JulyMetadataField`）。
- **删除**：`domain.lowcode011.model` 旧族（`MetaData011`/`FieldInfo011`/`DisplayInfo011`/`ServiceInfo011`）；`MetadataContentMapper` 只留 `from(JulyMetadata)`。**唯一 schema = 契约**。
- **校验**：门禁 PASS、`clean compile` **BUILD SUCCESS**。
- **E2E**：033 probe(10)/建模读(2)、低代码 getByObjectName(11/3/4)/load(11)/模板导出→上传 valid/alter/runtime query(4579)/deployObject(0.0.6)、AI chat —— 全过。
- **剩余（可选）**：期三 `ObjectQueryCommand`/`RowView` 深度定型。

## 任务三十一：042 期三（ObjectQueryCommand + RowView）收尾 —— 042 全部完成

- **`application.ObjectQueryCommand`**（objectName/filters/pageIndex/pageSize）：web 层从 body 构建，application 边界不再收裸 `Map`。
- **`application.RowView`**（`@JsonValue` → 扁平）：`ObjectTableGateway.query` 返回 `List<RowView>`，动态列收口到该值对象，wire 形状不变。
- **接入**：`ObjectTableGateway.query`、`JulyMetadataRuntimeUseCase.query`、`OpenApiUseCase.query` + 两个控制器（`command(body)` 辅助）。
- **E2E**：runtime query / open query 行仍为**扁平对象**（非 `{values:...}`），total 4579；getMeta 200；AI chat 200；`BUILD SUCCESS`、门禁 PASS、0 坏链。
- **042 全链完成**：S1 + 期一（含 033 收敛 + 删 `model/`）+ 期二 + 期三 + 期四。

## 台账

- 低代码文档统一在 `docs/lowcode011/`（011/013/015/016…030）。
- **042 完成**（全部四期）。
- **042 完成**（S1 + 期一~期四；仅 `ObjectQueryCommand/RowView` 未做）。
- 041（ai011 调用）立案；042 起可用。
- 本轮 1/2/4/5 代码已写（同步校验 / 动态审计 / 探针修伪列 / 物理表策略），**编译与 E2E 待用户允许**（用户要求先别编译）。
- 039：一期（设计）+ 二期（发布/数据同步）+ 开放 API + 运行时菜单/数据 + 元数据校验 完成；运行时权限模型 + 动态审计 挂起。
- 040 完成（元数据模板）；低代码文档统一在 `docs/lowcode011/`（011/013/015/016…029）；下一可用编号 **041**。
- 本地对象数据目录 `base-path` 仍为 `./storage011`（运行数据目录，非包名/路由，未动）。
- 默认桶口径：适配器配置声明（`local011.default-bucket` / `minio011.default-bucket`）→ 运行时经 `StorageDefaultsPort`；表仍是实例/连接权威（播种未动）。
- 未提交：包重命名 + 控制器收敛 + 对象/桶/实例出参对齐 + testConnection 补齐 + 默认桶配置 + 文档改动（等用户 commit 指令）。
