# 2026-09-15 工作日志

## 任务：规划 dataservice011 新域（文档阶段）

**状态**：文档已完成；**031 已编码**（后续同日完成，见下方各任务与 `docs/README.md` 台账）

### 已完成项

| # | 产出 | 路径 |
|---|------|------|
| 1 | 原始对话记录 | `docs/2026-09-15-chat.md` |
| 2 | 业务设计（requirement011） | `docs/requirement011/031.topic-dataservice011.md` |
| 3 | 技术方案（requirement013） | `docs/requirement013/031.topic-dataservice011.md` |
| 4 | DDL | `docs/sql/july_datasource.sql` |
| 5 | 修订过时的架构底册 | `docs/infrastructure011/017.topic-dynamic-datasource.md` |
| 6 | 编号台账登记 031 | `docs/README.md`（下一可用编号 → 033） |

### 调研结论

- `juydatasouce` 字面名全盘不存在，判定为 `july_datasource` 口头简写。
- 老项目完整三表实现在 `D:\aikbjava011\java17011\kj17-f011_20260718_a011`（包名即 `dataservice011`）。
- 本项目已有 877 行动态数据源设施（`domain.datasource` + `infrastructure.datasource`），yaml 驱动 + 懒加载。
- 原 `infrastructure011/017` 文档描述（"本期无管理端点 / 表驱动二期"）与代码实际能力不符，已修订。

### 本期范围（用户定案）

`dataservice011` 新域 + `july_datasource` 表驱动 + CRUD + 测试连接。
测试连接口径：临时池 `initialSize 0` / `maxActive 1` / `10s 超时`，**不回灌注册表**。

### 边界外

`july_sql`（SQL 注册）/ `july_model`（业务建模）推迟二期；密码加密存储二期（密钥配置位本期预埋，经 `KrtConfig011` + 环境变量，不新造存储介质）。

### 待处理项（需用户确认后执行）

技术方案 §019 已列 13 项任务拆解，核心为：

1. domain：`JulyDatasource` 聚合 + 仓储接口 + `ConnectionInfo`/`RegistryPort` 扩展
2. infra：Po/Mapper/RepositoryImpl + `DataSourceProbe011` + 注册表表驱动改造
3. application：`JulyDatasourceUseCase`
4. web：6 个 Vo011 + Converter（密码出站收口）+ Controller 10 端点
5. 质量闸门：门禁 + 编译 + 测试 + E2E（21 条验收清单）

### 技术约束备忘

- 新增代码收敛在 `dataservice011` 域包；`domain.datasource` / `infrastructure.datasource` 原地扩展（横切设施，非业务域）
- 密码字段从 `Vo011` **类型层面剔除**（不是置空）
- `dsCode` 保留字 `master`；不可变；驱动类名走 `DatabaseType011` 枚举
- reload 时**配置未变则保留旧池**，避免无关改动断连

---

## 任务：july_dictionary 数据字典（system011）

**状态**：设计文档 + **已编码并 E2E**（10 端点，见后续「035 数据字典编码」小节）

### 已完成项

| # | 产出 | 路径 |
|---|------|------|
| 1 | 原始对话记录（会话二） | `docs/2026-09-15-chat.md` |
| 2 | 业务设计（requirement011） | `docs/requirement011/035.topic-dictionary.md` |
| 3 | 技术方案（requirement013） | `docs/requirement013/035.topic-dictionary.md` |
| 4 | DDL | `docs/sql/july_dictionary.sql` |

### 决策定案（用户 2026-09-15）

1. 主子表；主表、子表都带 `sort_order`（升序）
2. 表名 `july_dictionary` + `july_dictionary_item`（单数）
3. 明细字段本期只 code/label/sort/status/remark
4. 每次查库不缓存（对齐 029）
5. 程序读入口 `getByType` / `getLabel` / `isValidItem` 要
6. 030 导出 Provider 要
7. 归属 `system011`

### 待处理项（需用户确认后执行）

技术方案 §019 已列 8 项任务拆解，核心为：

1. domain：`JulyDictionary` + `JulyDictionaryItem` 聚合 + 两个仓储接口
2. infra：2 Po + 2 Mapper + 2 RepositoryImpl（主表仓走 `BaseMasterSubRepository021`）
3. application：`JulyDictionaryUseCase` + 2 个 ExportProvider
4. web：8 个 Vo011 + Converter + Controller 9 端点
5. 质量闸门：门禁 + 编译 + E2E（16 条验收清单）
6. 同步 `docs/README.md` 台账

### 待用户拍板（1 项）

- 删除主表时仍存在存活明细：**默认 400 拒删**（先清明细），是否改为级联逻辑删除？

---

## 任务：julyDatasource 加排序 + 外部数据源驱动/入库验证

**状态**：已完成并 E2E 通过

### 变更内容

| # | 层 | 变更 |
|---|-----|------|
| 1 | DDL | `docs/sql/july_datasource.sql` 加 `sort_order INT NOT NULL DEFAULT 9999`；dev 库已 `ALTER TABLE` |
| 2 | infra | `JulyDatasourcePo` 改继承 `BasePo011`（= BasePo + sortOrder） |
| 3 | domain | `JulyDatasource` 加 `sortOrder`（create 默认 9999 / update 留空保持 / getter） |
| 4 | application | `JulyDatasourceUseCase.insert/update` 透传 sortOrder；`DatasourceSeed011` 传 null |
| 5 | web | Insert/Update/Vo011 加 `sortOrder`；Converter 映射；Controller 透传 |
| 6 | infra | `JulyDatasourceRepositoryImpl` 映射 + 列表改 `ORDER BY sort_order ASC, id ASC` |
| 7 | pom | 新增 `ojdbc8`（21.5.0.0）+ `mssql-jdbc`（12.8.1.jre11）驱动（parent BOM + infrastructure runtime） |

### E2E 实测（dev 库 + 真实外部库）

- 6 条外部数据源（Oracle×4 / SQL Server×2）`insert` 入库 200，`getById` 无 password，`testConnection` 全部 `success=true`（Oracle / Microsoft SQL Server）。
- `reloadRegistry`：enabled 8 / registered 8 / reused 8 / closed 0 / failed 0（配置未变保留旧池）。
- 排序：`sorttest011`(1) → `u8011`(2) → 9999 组（按 id 升序）；更新 u8011 留空密码后探测仍通。
- 门禁 224 文件 0 违规，编译 SUCCESS。

### 说明

- 生产 `krt.ci011` 6 条写入 `application-production.yml`（.gitignore 已挡，不入库）；外网/内网连通性用 `tmp/DatasourceProbe.java`（gitignore）独立实测。

---

## 任务：新域 ai011 · july_ai_model_provider（AI 模型接入）

**状态**：已完成并 E2E 通过（用户 2026-09-15 批准）

### 交付物

| # | 产出 |
|---|------|
| 1 | 文档：`requirement011/036.topic-ai011.md` · `requirement013/036.topic-ai011.md` · DDL `sql/july_ai_model_provider.sql` |
| 2 | domain `ai011`：`AiModelProvider` / `AiModelProviderApi` / 两仓储 / QuerySpec / `AiModelProbePort` |
| 3 | infra `ai011`：2 Po（BasePo011 + MasterLinked）/ 2 Mapper / 2 RepositoryImpl / `AiModelProbe011`（JDK HttpClient） |
| 4 | application `ai011`：`AiModelProviderUseCase` |
| 5 | web `ai011`：`AiModelProviderController`（11 端点）+ Converter + 9 Vo011；Swagger 组 `ai011` |
| 6 | 装配：主类 `@MapperScan` 加 `com.klsjnh.infrastructure.ai011.mapper` |

### 决策定案

主子表 + 都带排序；子表名 `july_ai_model_provider_api`；`models` 逗号分隔；密钥默认取首个启用；**api_key 出参不回显**（明文入库 / 加密二期）；testConnection 提供商级 + 密钥级；归属新顶层域 `ai011`。

### E2E 实测

- **建表 + 入库**：dev 库建两表；4 提供商（stepfun/longcat/deepseek/asterly）+ 6 密钥 insert 全 200。
- **排序**：列表按 `sort_order` 1/2/3/4 升序。
- **脱敏**：`selectApiListByProvider` 出参**无 apiKey 字段**；`getById` 正常。
- **约束**：重复 providerCode → 400；同 provider 重复 apiCode → 400；有密钥删 provider → 400。
- **探测**：`testConnection` stepfun/longcat/deepseek = `HTTP 200` success；asterly = 连接超时 → `success=false`（HTTP 恒 200）；密钥级 `testConnectionApi` 正常。
- **保持原值**：`updateApi` 留空 apiKey 后密钥级探测仍 `HTTP 200`。
- 门禁 250 文件 0 违规，编译 SUCCESS。

### 说明

- 真实 api_key 仅写 dev 库 / 运行入参，**未落入任何入库文档**；生产敏感数据后续走 `KrtConfig011` 密钥位加密（二期）。
- 边界外（未做）：密钥加密、多密钥轮询、模型级子表、缓存、030 导出。

---

## 任务：P0 缺陷修复（用户 2026-09-15 批准）

| # | 修复 | 验证 |
|---|------|------|
| 1 | `GlobalExceptionHandler`：`DuplicateKeyException → 400` | 逻辑删除后重插 apiCode/providerCode → 400（原 500） |
| 2 | `GlobalExceptionHandler`：`NoResourceFoundException → 404`（不再 ERROR 堆栈） | `/favicon.ico` → 404 |
| 3 | `AiModelProviderUseCase.testConnection` 停用语义一致 | 停用 provider 仍可探测（HTTP 200） |
| 4 | `AiModelProviderApi.update` 去掉 `"keep"` 占位 hack；create 独立校验 apiKey 必填 | 空 key 新增 → 400；留空更新保持原值 |
| 5 | `AiModelProvider` baseUrl 加 `http(s)://` 校验 | `example.com` → 400 |

> 另发现：`script011.sh gate` 的 `mvn -o compile` 在 WSL 上增量编译可能假过，建议改 `clean compile`（未动）。

---

## 任务：035 数据字典编码（用户 2026-09-15 批准）

**状态**：已编码并 E2E 通过（10 端点，含 `/export`）

- domain：`JulyDictionary` / `JulyDictionaryItem` / `JulyDictionaryBundle` + 两仓储 + QuerySpec
- infra：2 Po（BasePo011 / MasterLinked）+ 2 Mapper（system011.mapper，已扫描）+ 2 RepositoryImpl
- application：`JulyDictionaryUseCase`（CRUD + 程序入口 getByType/getLabel/isValidItem）+ `JulyDictionaryExportProvider`
- web：`JulyDictionaryController` + Converter + 8 Vo011；system011 Swagger 组覆盖
- E2E：insert/更新/分页/明细有序/`(pk_mt,item_code)` 唯一/删除约束/导出（带 token）全过；门禁 274 文件 0 违规
- 程序读入口（getByType/getLabel/isValidItem）无 HTTP，编译验证，未运行期直测

---

## 任务：ai011 导出 + 019 文档 + 033 业务建模编码（用户 2026-09-15 批准）

### 1. ai011 030 导出
- `AiModelProviderExportProvider`（objectCode `julyAiModelProvider`，**只导 provider，不含密钥**）+ 控制器 `/export`；E2E rows=4。

### 2. `docs/019.backend-api-review.md`
- 落盘：Swagger 可信度（缺 SecurityScheme / debug 差异 / 导出需操作人）、鉴权口径、已知缺口、新端点自检清单；README 台账登记。

### 3. 033 业务建模（Step 3/4 补全）
- infra：`JulyBusinessModelingPo`/`Mapper`/`RepositoryImpl` + `ModelDataCodec011`（JSON↔MetaData011，notNull 反转）+ `FieldTypeInferrer011`（12 值推断 + 公共列补齐）+ `BusinessModelingProbe011`（读 ResultSetMetaData）；infrastructure 加 jackson-databind。
- 分层修正：新增 domain `FieldInferencePort`（推断器在 infra，经端口暴露，避免 application→infra）。
- application：`JulyBusinessModelingUseCase`（CRUD + probe + executeSql + executeSqlByPage + getModelData）+ `BusinessModelingProbeResult`。
- web：`JulyBusinessModelingController`（11 端点）+ Converter + 11 Vo011。
- **字段改名定案**：`sql_text`/`sqlText` → **`sql_content`/`sqlContent`**（全链路 + dev 库 ALTER）。
- **列类型定案**：`sql_content` / `model_data` 由 `MEDIUMTEXT` → **`TEXT`**（3000–6000 字符量级，64KB 足够；两列大文本不用 VARCHAR 防撞行上限）。

### E2E 实测
- probe 推断：`SELECT code, data FROM july_config` → `id,code,data,status,create_by,update_by,create_time,update_time,dr`，类型/顺序/必填全部符合 §029；`dr`→`status`、`id`→`id`。
- 三条硬校验（非 SELECT / 含 `;` / 含 DROP）→ 400。
- insert/getById/getByCode/getModelData（无 vuePath/javaPath）→ 200；不存在 dsCode → 400。
- executeSql → columns+rows；executeSqlByPage pageSize 999999→clamp 500、1→clamp 10；logicDelete → getById 404。
- 门禁 297 文件 0 违规，编译 SUCCESS。

---

## 任务：存储中心管理面 + 在线编辑（迁移方案）

**状态**：方案已落（requirement011/013 037），**未编码**，待用户定案 §023 六项

- 老参照：`kj17-f016_20260719_a011`（管理面 + `EditableTextPolicy` 在线编辑）。
- 本项目缺口：只有端口 + local011/minio011 双适配器，缺表驱动多实例、实例/桶/对象管理、在线编辑。
- 产出：`docs/requirement011/037.topic-storage-migration.md`、`docs/requirement013/037.topic-storage-migration.md`、`docs/sql`（DDL 草案在 013 内）；更新 `infrastructure011/019`；README 台账登记 037（下一可用 038）。
- 待定案：配置模型 / provider 口径 / sort_order / 预签名 / 端口形态 / 编辑器种类。
