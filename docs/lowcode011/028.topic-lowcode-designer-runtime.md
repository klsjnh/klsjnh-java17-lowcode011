# 028.topic-lowcode-designer-runtime（低代码设计器与运行时）

> 阶段：**requirement011（业务设计与业务要求）**。
> 决策定案（2026-09-17）：**全链路做** —— 设计 → 探测 → 发布建表（**DDL 执行要**）→ 数据初始化/同步 → 运行时 → 开放 API；保留 **一主三子（预留多子）**；探测**复用 dataservice011**；发布采用**快照 + DRAFT/PUBLISHED 分离**；DDL **首次 `CREATE TABLE IF NOT EXISTS`、之后仅 `ALTER`、禁 `DROP`**；**运行时权限模型单列**；apiKey 打码/轮换；运行时页面要做。小步快跑，**分三期**。
> 基础：[038.topic-lowcode-core](027.topic-lowcode-core.md)（一主三子元数据 CRUD）。
> 技术方案：[requirement013/039.topic-lowcode-designer-runtime](../requirement013/039.topic-lowcode-designer-runtime.md)

## 011.对象定义（ObjectInfo011）

| param011   | param013                                                     | remark011 |
| ---------- | ------------------------------------------------------------ | --------- |
| objectCode | july_metadata（038 主，加发布态列）+ **july_metadata_version**（发布快照）+ **july_metadata_open_api**（开放 API 配置） | 038 三子沿用 |
| objectName | 低代码核心 - 设计器与运行时                                    | 归属 `lowcode011` 域 |
| objectUrl  | /klsjnh/lowcode011/julyMetadata/v1                            | Swagger 组 `lowcode011`；设计器/发布/数据/运行时/开放 API 全部挂此前缀 |

## 013.能力全景（对齐老 runtime 23 端点）

| 组 | 端点 | 期 |
|----|------|----|
| 设计 | `listModels` `load` `save` `probeSql` `buildFromProbe` `probeAndInfer` `saveDraft` `publishMeta` `selectListByPage` `getById` | 一 |
| 预览 | `previewDdl` | 一 |
| 发布 | `publish`（建表 / ALTER） | 二 |
| 数据 | `syncData` `importDataFromSql` `importStatus` | 二 |
| 运行时 | `publishMenu` `listRuntimeMenus` `initialize` + 动态 CRUD（`/runtime/<objectName>`） | 三 |
| 开放 API | `openApiConfig` `saveOpenApiConfig` `rotateApiKey` + `/klsjnh/open/v1/<objectName>` | 三 |

## 013.1 进展（2026-09-17 收口）

| 能力 | 状态 | 说明 |
|------|------|------|
| DDL 增量（`july_metadata` 发布态列 / `july_metadata_version` / `july_metadata_open_api`） | ✅ | 已建并执行到 dev |
| 设计：`listModels` `load` `save` `previewDdl` | ✅ | 一主三子 ↔ MetaDTO；DDL 生成器与发布共用 |
| 设计：`probeSql` `buildFromProbe` `probeAndInfer` `saveDraft` `publishMeta` | ⏳ | 探测走 033 端点复用；这四个 039 侧**未单独暴露** |
| 发布：`publish`（首建 CREATE / 后仅 ADD COLUMN / 快照 / 版本 / 开关） | ✅ | `bpm_user011` → `bpm_user011` 三路径 E2E |
| 数据：`importDataFromSql` `importStatus` | ✅ | 分页读 → 按业务键 `businessField` upsert（幂等） |
| 数据：`syncData` | ⏳ | 暂以分页循环代替 |
| 开放 API：`openApiConfig` `saveOpenApiConfig` `rotateApiKey` + `/klsjnh/open/v1/`（meta/query/insert/update/delete） | ✅ | api_key 鉴权、公共 CRUD、meta 查询 |
| 运行时：`publishMenu` `listRuntimeMenus` + `/runtime` 运行数据面 | ✅ | 菜单落 `july_menu`（`rt_<object>` → `/runtime/<object>`，幂等）+ JWT 动态 CRUD（meta/query/insert/update/delete） |
| 运行时：`initialize` | ➖ | 老版本是建元数据表；本项目表由 `docs/sql` 迁移管理，**不适用** |
| 运行时权限模型（对象级 + 操作级） | ⏳ | 单列，未做 |
| 运行时按元数据校验（required/类型/长度） | ✅ | `MetadataValueValidator`：insert 校验必填/类型/长度，update 仅校验出现的字段 |
| 动态 objectCode 审计（运行时/开放 API 写操作） | ✅ | 写操作落 `july_user_audit`，`object_code = objectName`（运行时取 JWT 操作者；开放 API 记 `open-api`） |
| `/runtime` 运行页（前端） | ➖ | 前端另行开发（后端已提供数据面 + 菜单） |
| 开放 API 限流 | ⏳ | 未做 |

## 015.三条核心决策（2026-09-17 定案）

1. **发布快照 + 编辑态分离**：物理表结构由**发布那一刻**的元数据驱动，落 `july_metadata_version`（`object_name + version + payload_json`(MetaDTO 快照) + `physical_table` + `ddl_text` + `published_by` + `published_at`）。`july_metadata` 是 DRAFT 编辑态（保留 `publish_status` / `version` 作当前指针）；发版后继续改元数据**不动已发布快照**。
2. **DDL 边界**：首次发布 = `CREATE TABLE IF NOT EXISTS`；其后**仅 `ALTER`**（`ADD COLUMN` / `ADD INDEX` / `ADD UNIQUE`）；**禁 `DROP` / `DROP COLUMN` / `MODIFY` / `RENAME`**（列类型/长度变更走「新列 + 数据迁移」，二期评估）。**对象表名 = 对象名**（不加前缀，2026-09-18 定），建议隔离库/schema；发布幂等、可重入；带开关 `krt.lowcode.ddl-execute.enabled`（默认 false）。
3. **运行时权限模型单列**（对象级 + 操作级），不夹在运行时段落里做；本期运行时先按「已发布对象 + 白名单操作」收敛，权限模型随后单独一期。

## 017.业务规则

- **探测**：只读连接 + 超时 + `maxRows`；复用 `dataservice011` 的 `executeSql`/探针，按返回推断字段类型（`inferFieldData`）。
- **元数据校验**：`displayCode` 必须绑定已存在 `fieldCode`；类型过 `FieldType011`/`ObjectType011`；`objectName` 唯一不可变。
- **发布**：建表/ALTER + 版本快照 + 发布指针；幂等可重入（**无备份表、无 PENDING 流程、无 BACKUP 审计** —— 与实现一致）。
- **运行时 CRUD**：仅已发布对象；字段白名单 + 全绑定参数 + 服务端按元数据校验（required / 类型 / 长度 / 唯一）+ 动态 `objectCode` 审计。
- **开放 API**：`apiKey` 打码出参、仅轮换首次回明文一次；`allowedOps` 白名单；限流。

## 018.业务字段映射平台基列（B 口径，2026-09-17 定案）

**场景**：tuser 建模要加 `status` 并映射到系统现有 `status` 列。

- **允许（B 口径）**：业务字段直接落到平台基列 —— 导入 SQL 带上该列（`select ... , status from tuser`），同步按**列名大小写不敏感匹配**写入目标基列。**无需加元数据字段、无需改代码**。
- **禁止**：在元数据里再加一个同名列（哪怕仅大小写不同）——`july_metadata_field` 唯一键 `(pk_mt, field_code)` 排序规则**大小写不敏感**，`STATUS` 与 `status` 撞键直接 400。
- **代价（已知并接受）**：语义合并 —— 平台 `status`（启用/停用 0/1）被业务状态（如 1/2）占用；admin 启停与数据同步会互相覆盖。平台**删除位是 `dr`**，不受影响。
- **类型**：目标列类型以基列为准（`status VARCHAR(3)`），业务值需能装下。
- **实测（2026-09-17）**：`bpm011.tuser` 4579 行，导入后 `bpm_user011.status` 分布 1:1822 / 2:2757，与源一致；元数据/代码零改动。
- **若要“真映射”**（源列名 ≠ 目标列名且不用别名）：需新增 `sourceColumn → targetColumn` 映射配置 + 同步按映射落列 —— 当前模型没有，列后续。

### 018.1 外键 / 引用列（`pk_xxx`，固定 `VARCHAR(33)`）

- **约定**：引用其他对象的列命名 `pk_<实体>`（如 `pk_organization`），类型**固定 `VARCHAR(33)`**（对齐平台 `id`）。
- **用法（A 路径，无需改代码）**：设计器加字段（`fieldType=string, length=33`）→ `publish` 检测缺列自动 `ALTER TABLE ... ADD COLUMN` → 导入 SQL 用**别名**把源列映射进来（`orgid pk_organization`）。
- **实测（2026-09-17）**：`bpm011.tuser` 4579 行，源 `orgid` 非空 4566（13 空）→ `bpm_user011.pk_organization` 非空 4566，**与源一致**；版本 0.0.2（ALTER）。

### 018.2 开放 API（api_key 公共 CRUD + meta，2026-09-17 落地）

**管理端（JWT，挂 `/klsjnh/lowcode011/julyMetadata/v1`）**
- `GET /openApiConfig?objectName=`：读配置（apiKey 打码）。
- `POST /saveOpenApiConfig`：`{objectName, enabled, authMode(closed/none/apiKey), allowedOps[]}`；首次生成 apiKey 时**回明文一次**。
- `POST /rotateApiKey`：`{objectName}` 轮换，**回明文一次**。

**运行时段（JWT）** — `2026-09-17 落地`
- `POST /publishMenu`：`{objectName, parentMenuCode?}` 幂等发布运行时菜单（`july_menu` 建 `rt_<object>` → `/runtime/<object>`）。
- `GET /listRuntimeMenus`：列路由在 `/runtime/` 下的已发布对象（含 version/publishStatus）。
- `/klsjnh/lowcode011/runtime/v1`：`GET /meta?objectName=`、`POST /query|insert|update|delete`（`{objectName, ...}`，JWT 动态 CRUD，列白名单 + 全绑定 + 补基列默认）。

**开放端（`X-Api-Key` 头；JWT 已白名单 `/klsjnh/open/`）**
- `GET  /klsjnh/open/v1/getMeta?objectName=`：对象元信息（MetaDTO）。
- `POST /klsjnh/open/v1/query`：`{objectName, filters, pageIndex, pageSize}` 分页。
- `POST /klsjnh/open/v1/insert`：`{objectName, ...列}`。
- `POST /klsjnh/open/v1/update`：`{objectName, id|sid, ...列}`。
- `POST /klsjnh/open/v1/delete`：`{objectName, id|sid}`（有 `dr` 走逻辑删）。

**规则**
- 仅**已发布**对象可用（物理表取最新快照）；表/列以**库表目录为白名单**，值全绑定；`insert` 自动补基列默认（id/status/dr/时间）。
- 鉴权顺序：配置存在且 `enabled` → `authMode=apiKey` 校验 `X-Api-Key` → `allowedOps` 含当前操作；不通过 401。
- apiKey：32 位 hex，读打码 `****xxxx`，生成/轮换明文仅回一次。
- 项目 URL 规约：**禁 path variable**，objectName 走 query/body。

## 019.分期（小步快跑，每期可演示/上线/回滚）

| 期 | 主题 | 一条竖切 | 端点 | 状态 |
|----|------|----------|------|------|
| **一** | 设计闭环（**零改库**） | 建模 → 探测 → 预览 DDL | `listModels` `load` `save` `previewDdl`（已做）；`probeSql` `buildFromProbe` `probeAndInfer` `saveDraft` `publishMeta`（未单独暴露） | ◐ |
| **二** | 发布闭环（写库） | `previewDdl → publish → 导入` 一个对象跑通 | `publish` `importDataFromSql` `importStatus`（已做）；`syncData`（以分页循环代替） | ✅ |
| **三** | 运行时 + 开放 API | `publishMenu → 运行页 CRUD → 开放 API` | 开放 API `openApiConfig` `saveOpenApiConfig` `rotateApiKey` + `/klsjnh/open/v1/`（已做）；`publishMenu` `listRuntimeMenus` `initialize` + `/runtime` 运行页 + 权限模型（未做） | ◐ |

> 数据模型**一期一次到位**（建 `july_metadata_version` / `july_metadata_open_api` 与 `july_metadata` 发布态列），避免二期 ALTER 折腾；`previewDdl` 与 `publish` **共用同一 DDL 生成器**（一期落地）。

## 021.边界外（本期不做）

- 子表类型扩展（「多子」本期仍固定 field/display/service 三类）；
- 033 → 本模块的元数据导入；
- 行级数据权限 / 字段级脱敏；
- 生产 DDL 自动执行（需 DBA 审核/工单，开关默认关）；
- 对象间关联/外键。

## 022.后续与取舍（2026-09-17 用户口径）

- **交付重点 = 核心链路**：设计 → 发布建表 → 数据同步 → 元数据模板 → 开放 API（均已 E2E）。
- **后续功能价值有限，保持现状、不再扩展**（用户评估）：运行时权限模型、真字段映射（`sourceColumn→targetColumn`）、开放 API 限流/CORS、`syncData`、模板批量 `templates[]`、`/runtime` 运行页（前端）。（动态 `objectCode` 审计**已实现**，不再挂起。）
- 挂起项按需再启，不影响已交付链路。

## 023.决策记录（2026-09-17 定案）

| # | 决策点 | 结论 |
|---|------|------|
| 1 | 范围 | ✅ 全做（含 DDL 执行） |
| 2 | 数据模型 | ✅ 保留一主三子（预留多子，本期固定三类） |
| 3 | 探测 | ✅ 复用 dataservice011 |
| 4 | 发布安全 | ✅ 快照 + 版本 + 白名单；**首建表、后仅 ALTER、禁 DROP**；无状态幂等（无备份表 / 无审计）；开关默认关 |
| 5 | 开放 API | ✅ apiKey 打码 / 轮换 |
| 6 | 运行时 | ✅ 要做（动态 CRUD + 运行页）；**进展**：动态 CRUD 已由开放 API 覆盖，运行页/菜单待做 |
| 7 | 运行时权限 | ✅ 单列（对象级 + 操作级），不夹在三期 |
| 8 | 交付 | ✅ 分三期，小步快跑 |
| 9 | 业务字段映射平台基列 | ✅ B 口径：导入 SQL 带列即落基列（无字段、无代码改动）；同名列（含大小写变体）禁加；语义合并代价已知 |
| 10 | 外键/引用列命名 | ✅ `pk_<实体>`，固定 `VARCHAR(33)`；A 路径（加字段 → ALTER ADD → 导入别名映射），无代码改动 |
| 11 | 开放 API | ✅ api_key 鉴权 + 公共 CRUD + meta 查询；配置/轮换回明文一次；`/klsjnh/open/v1` JWT 白名单 |
