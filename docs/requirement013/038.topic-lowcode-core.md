# 038.topic-lowcode-core（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/038](../lowcode011/027.topic-lowcode-core.md)（一主三子 / 都带排序 / 仅 CRUD）。
> 定位：把低代码对象元数据**一主三子**落库并 CRUD。
> **2026-09-17（042）更新**：原「**不改** 033 依赖的 `domain.lowcode011.model` 值对象」约束**已显式解除** —— 该族已删除，`JulyMetadata` 改为**组合**共享契约 [`MetadataContent`](../lowcode011/013.topic-metadata011.md)。
> 延伸：[039.topic-lowcode-designer-runtime](039.topic-lowcode-designer-runtime.md) 在其上补「设计 → 发布建表 → 数据同步 → 开放 API」（运行时菜单/运行页挂起），038 的「仅 CRUD」是**当时边界**，现状见 039。

## 011.技术落点（类清单）

```
java17-domain011（新增，包 com.klsjnh.domain.lowcode011）
  JulyMetadata                 主聚合根（objectName 不可变 / sortOrder / 三子持有）
  JulyMetadataField            子1 record（fieldCode...sortOrder + 校验）
  JulyMetadataDisplay          子2 record（displayCode...sortOrder + 校验）
  JulyMetadataService          子3 record（serviceCode...sortOrder + 校验）
  JulyMetadataRepository       仓储接口（insert/update/findById/findByObjectName/logicDeleteById/findPage/count）
  JulyMetadataQuerySpec        查询规格（keyword / objectType / status）

java17-infrastructure011（新增，包 com.klsjnh.infrastructure.lowcode011）
  entity/JulyMetadataPo                extends BasePo011，@TableName("july_metadata")
  entity/JulyMetadataFieldPo            extends BasePo011 implements MasterLinked（pk_mt）
  entity/JulyMetadataDisplayPo          同上
  entity/JulyMetadataServicePo          同上
  mapper/JulyMetadata*Mapper            BaseMapper + @Delete deleteByMaster（子表整替物理删）
  repository/JulyMetadataRepositoryImpl extends BaseRepository（master CRUD + 三子级联整替）

java17-application011
  com.klsjnh.application.lowcode011.JulyMetadataUseCase   CRUD（@Transactional）

java17-web011
  com.klsjnh.web.lowcode011.controller.JulyMetadataController   7 端点
  com.klsjnh.web.lowcode011.converter.JulyMetadataConverter
  com.klsjnh.web.lowcode011.vo.*                                SaveVo / QueryVo / Vo011 / 三子 Vo011
```

## 013.关键机制

- **主子整替**：update = 更新主 → 物理删三子 → 重插三子；insert = 插主 → 插三子；
- **级联逻辑删除**：logicDelete = 物理清三子 → 逻辑删主（`@TableLogic`）；
- **子行物理删**：三子 Mapper 加 `@Delete("DELETE FROM ... WHERE pk_mt = #{masterId}")`，绕开 `@TableLogic`——因唯一键 `(pk_mt, code)` 不含 `dr`，逻辑删后重插会同键冲突（实测 400 duplicate key，已修）；
- **校验**：`objectName` 唯一；子 `code` 同对象内唯一；`displayCode` 必须命中一个 `fieldCode`；类型过枚举。

## 015.DDL 与端点

- DDL：`docs/sql/july_metadata.sql`（4 表，`id VARCHAR(33)`、`sort_order`、审计四列、`dr`）。
- 端点前缀 `/klsjnh/lowcode011/julyMetadata/v1`：`insert` / `update` / `logicDelete` / `logicDeleteBatch` / `getById`(GET) / `getByObjectName`(GET) / `selectListByPage`（7 个）。
- Swagger 组：新增 `lowcode011GroupedOpenApi`（`/klsjnh/lowcode011/**`）。

## 017.与现状的改造点

| # | 改造 | 说明 |
|---|------|------|
| 1 | 主类 `@MapperScan` 加 `com.klsjnh.infrastructure.lowcode011.mapper` | 装配 |
| 2 | `SpringDocConfig011` 加 `lowcode011` 组 | 文档 |
| 3 | `BackupUseCase` 由 `ObjectStoragePort` 直接注入改为 `StorageResolverPort` | 037 表驱动改造的连带修复（无单例 `ObjectStoragePort` bean 了） |

## 019.任务拆解（已完成，2026-09-16）

| # | 任务 | 状态 |
|---|------|------|
| 1 | DDL `july_metadata.sql` + dev 建表 | ✅ |
| 2 | domain 主 + 三子 record + 仓储 + QuerySpec | ✅ |
| 3 | infra 4 PO + 4 Mapper + 聚合仓储（整替/级联） | ✅ |
| 4 | application `JulyMetadataUseCase` | ✅ |
| 5 | web Controller + Converter + VOs | ✅ |
| 6 | 门禁 + 编译 + E2E | ✅ |

## 021.E2E 实测（助手自测，2026-09-16）

- insert（主+3子）→ 200；`getById` 含三子且按 `sortOrder`；`getByObjectName` 200；
- 重复 `objectName` → 400；display 绑定未知字段 → 400；
- update 三子整体替换（删 amount、加 status、清 service）→ 200，回读一致；
- logicDelete 级联 → `getById` 404，子行物理清 0；
- 门禁 0 违规，编译 SUCCESS。

## 023.风险 / 边界

- 子表物理删除 = 子行无历史（整替语义的代价，已定案）；
- 与 033 尚未打通导入（边界外）；
- 大 `service_content` 为 `TEXT`（原 `VARCHAR(20000)` 超 utf8mb4 上限，已改）。
