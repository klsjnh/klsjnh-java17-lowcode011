# 021.topic-july-scheduler（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 `requirement011/021.topic-july-scheduler.md`（业务规则，含三条已定案决策）与 `infrastructure011/`（架构分层）。
> 编码以本文件为准；未列出的能力一律不做（边界见 011 §021）。

## 011.调度引擎选型：Quartz 内存模式

- 依赖：`spring-boot-starter-quartz`（java17-infrastructure011 引入，版本由 Boot 3.4.5 BOM 管理 = Quartz 2.5.0）
- JobStore：**RAMJobStore（内存）**——`july_scheduler` 表本身是任务事实源，应用启动时按 `status='1'` 重新注册；不引入 JDBC JobStore 的 11 张 QRTZ_* 表
- 引擎 API ↔ 业务动作映射：启动=`scheduleJob`；停止=`deleteJob`；执行一次=`triggerJob`（一次性触发器）；改 cron=`rescheduleJob`；校验=`CronExpression.isValidExpression`
- TaskScheduler 否决：无按名摘除 / 立即触发 / misfire 策略

## 013.DDL（july_scheduler，建库 dev202609120001）

```sql
CREATE TABLE july_scheduler (
    id                 VARCHAR(33)  NOT NULL                COMMENT '主键',
    status             VARCHAR(3)   NOT NULL DEFAULT '0'    COMMENT '运行态（0 停止 / 1 运行）——本表业务约定默认 0',
    create_by          VARCHAR(33)  NULL                    COMMENT '创建人',
    update_by          VARCHAR(33)  NULL                    COMMENT '最后修改人',
    create_time        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建日期',
    update_time        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后修改日期',
    dr                 VARCHAR(3)   NOT NULL DEFAULT '0'    COMMENT '删除标记（0 正常 / 1 已删除）',
    scheduler_code     VARCHAR(30)  NOT NULL                COMMENT '任务编码',
    scheduler_name     VARCHAR(60)  NOT NULL                COMMENT '任务名称',
    scheduler_handler  VARCHAR(300) NOT NULL                COMMENT '处理器内容（Spring Bean 名）',
    scheduler_cron     VARCHAR(30)  NOT NULL                COMMENT 'cron 表达式',
    execute_times      INT          NOT NULL DEFAULT 0      COMMENT '执行次数（触发即计）',
    PRIMARY KEY (id),
    UNIQUE KEY uk_scheduler_code (scheduler_code),
    KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统管理-定时任务';
```

- `status DEFAULT '0'`：本表业务例外（新增默认停止），偏离公共模板的 '1'，已由 011 §023-1 定案
- `uk_scheduler_code`：业务唯一兜底（应用层预检仅是友好提示）

## 015.类设计

```
java17-common011
  com.klsjnh.common.enums.Status011                     status 列取值枚举（ENABLED '1' / DISABLED '0'）——状态比较禁用魔法值
java17-domain011
  com.klsjnh.domain.system011.scheduler.JulyScheduler               聚合：EntityId + 业务字段 + AuditInfo + 运行态
  com.klsjnh.domain.system011.scheduler.JulySchedulerRepository     仓储接口
java17-application011
  com.klsjnh.application.system011.scheduler.JulySchedulerUseCase   8 个用例方法，@Transactional；引擎调用在事务提交语义内幂等（内存引擎可随时从 DB 重建）
java17-infrastructure011
  com.klsjnh.infrastructure.persistence.entity.JulySchedulerPo        extends BasePo
  com.klsjnh.infrastructure.persistence.mapper.JulySchedulerMapper    extends BaseMapper<JulySchedulerPo>
  com.klsjnh.infrastructure.persistence.repository.JulySchedulerRepositoryImpl
      extends BaseRepository<T,M> & implements JulySchedulerRepository
      getBusinessColumn() = "scheduler_code"
  com.klsjnh.infrastructure.system011.scheduler.SchedulerEngine                 Quartz 引擎（SchedulerPort 实现）
  com.klsjnh.infrastructure.system011.scheduler.JobHandlerRegistry              JobHandler 注册器（收集容器内全部 JobHandler Bean）
  com.klsjnh.domain.system011.scheduler.JobHandler                    ★ Handler 契约（handlerName + execute(payload)）——demo：app011 Demo011Scheduler
  com.klsjnh.infrastructure.system011.scheduler.SchedulerHandlerJob             Quartz Job：经 JobHandlerRegistry 解析 handler 执行 + execute_times 自增
  com.klsjnh.infrastructure.system011.scheduler.SchedulerBootstrap              ApplicationReadyEvent 监听：按 status='1' 重注册（重启自愈）
java17-web011
  com.klsjnh.web.system011.controller.JulySchedulerController         8 端点（见 011 §019）
  com.klsjnh.web.system011.vo                                         请求/响应 VO（@Schema 中文）
```

> status 运行态约定：`Status011.ENABLED`（'1'）= 运行、`DISABLED`（'0'）= 停止——业务代码一律用枚举，不出现 '0'/'1' 字面量。

### 015.1 handler 解析规则

`scheduler_handler` = **Spring 容器内的 Bean 名**，Bean 本身实现 `java.lang.Runnable`。触发时：
`applicationContext.getBean(handler, Runnable.class).run()`——Bean 缺失或执行异常：`execute_times` 照常 +1（011 §023-2 触发即计），记 WARN 日志，**不影响调度继续**。

### 015.2 引擎联动规则（update）

update 保存成功后**无条件重同步引擎**（幂等）：先 `deleteJob` 摘除旧注册，再按新 status 决定——'1'（运行）→ `scheduleJob` 重新注册（cron/handler 变更等效于改周期重注册）；'0'（停止）→ 仅摘除。start / stop 端点是同一条重同步逻辑的快捷入口（只翻转 status）。

### 015.3 引擎一致性说明（已知简化）

内存引擎操作在事务方法内执行；若事务回滚，引擎状态可能与 DB 短暂不一致——由"重启自愈 + 下次启动重注册"兜底，本期接受。

## 017.端点与出入参

| 端点（前缀 /klsjnh/system011/julyScheduler/v1） | 请求 Body | 出参 data |
|------------------------------------------------|-----------|-----------|
| /insert                                        | InsertVo：schedulerCode / schedulerName / schedulerHandler / schedulerCron（JSON 键 camelCase） | IdVo011 |
| /update                                        | UpdateVo：id + 四业务字段 + **status**（含启停） | IdVo011 |
| /logicDelete                                   | ids: String[]（批量） | BatchDeleteResultVo011 |
| /getById                                       | id | JulySchedulerVo（全字段 + 审计） |
| /selectListByPage                              | pageIndex / pageSize / schedulerCode / schedulerName（模糊） | PageResult011 |
| /start · /stop · /runOnce                      | id | IdVo011 / IdVo011 / IdVo011 |

- JSON 键 camelCase（docs/016）；信封统一 Response011
- 保存时 `CronExpression.isValidExpression` 校验，非法 → badRequest

## 019.依赖与配置增量

- `java17-infrastructure011/pom.xml`：+ `spring-boot-starter-quartz`
- `application.yml`：未显式声明 `spring.quartz.job-store-type`，使用 Boot 默认内存存储（RAMJobStore）

## 021.编码任务清单（已全部完成）

1. ✅ DDL 在 dev202609120001 建表（july_scheduler）
2. ✅ infrastructure：Po / Mapper / RepositoryImpl / SchedulerEngine / SchedulerHandlerJob / SchedulerBootstrap + quartz 依赖
3. ✅ domain：JulyScheduler 聚合 + JulySchedulerRepository 接口
4. ✅ application：JulySchedulerUseCase（@Transactional）
5. ✅ web：JulySchedulerController + VO
6. ✅ 验证：打包启动 → doc.html 看到接口 → insert → start → runOnce 观察日志与 execute_times
