# 031.topic-dataservice011（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/031](../requirement011/031.topic-dataservice011.md)（业务规则，五项决策已定案）与 [infrastructure011/017](../infrastructure011/017.topic-dynamic-datasource.md)（动态数据源横切设施）。
> 参照实现：`D:\aikbjava011\java17011\kj17-f011_20260718_a011` —— **借鉴其能力边界，不照搬其类设计**（差异见 §017）。
> 命名口径：主题文件统一 `031.topic-dataservice011`（新域名）；对象名 `JulyDatasource` / 表名 `july_datasource` 保持不动。

## 011.技术落点（类清单）

新增代码一律收敛在 **`dataservice011`** 域包内；已有 `domain.datasource` / `infrastructure.datasource` **原地扩展**（那是横切设施，不属业务域）。

```
java17-domain011
  com.klsjnh.domain.dataservice011.JulyDatasource               聚合根（dsCode 不可变 / 保留字校验 / 驱动解析）
  com.klsjnh.domain.dataservice011.JulyDatasourceRepository     仓储接口（findByCode / findAllEnabled / findPage / count）
  com.klsjnh.domain.dataservice011.JulyDatasourceQuerySpec      查询规格（keyword / status）
  com.klsjnh.domain.datasource.DynamicDataSourceRegistryPort    ★ 扩展：补 reloadAll(List<ConnectionInfo>)
  com.klsjnh.domain.datasource.ConnectionInfo                   ★ 扩展：补 schemaName / driverClass / poolConfig
  com.klsjnh.domain.datasource.ReloadResult                     新增：reloadAll 回报（enabled / registered / reused / closed / failed）
  com.klsjnh.domain.datasource.DataSourceProbePort              新增：探测端口 + ProbeResult（success / message / databaseProduct / databaseVersion）

java17-infrastructure011
  com.klsjnh.infrastructure.dataservice011.entity.JulyDatasourcePo        extends BasePo，@TableName("july_datasource")
  com.klsjnh.infrastructure.dataservice011.mapper.JulyDatasourceMapper    extends BaseMapper
  com.klsjnh.infrastructure.dataservice011.repository.JulyDatasourceRepositoryImpl
                                                                          extends BaseRepository（business column = ds_code）
  com.klsjnh.infrastructure.datasource.DataSourcePools011                 ★ 扩展：buildForProbe 统一 new / maxActive 1 / maxWait 10s；新增 isBuilt()；driverClass 空则取 DatabaseType011 默认
  com.klsjnh.infrastructure.datasource.DynamicDataSourceRegistryImpl      ★ 改造：表驱动 reloadAll()（表 → 池），保留 ci011 启动基线
  com.klsjnh.infrastructure.datasource.DataSourceProbe011                 新增：临时池探测（实现 DataSourceProbePort，不回灌注册表）

java17-application011
  com.klsjnh.application.dataservice011.JulyDatasourceUseCase             CRUD + logicDeleteBatch + testDraft/testSaved + reloadRegistry（@Transactional 只在此层）

java17-web011
  com.klsjnh.web.dataservice011.controller.JulyDatasourceController       9 管理端点
  com.klsjnh.web.dataservice011.converter.JulyDatasourceConverter         聚合 ↔ Vo011（密码出站口：目标 VO 无 password 字段）
  com.klsjnh.web.dataservice011.vo.julydatasource.JulyDatasourceVo011         出参（无 password 字段）
  com.klsjnh.web.dataservice011.vo.julydatasource.JulyDatasourceInsertVo011   入参（新增）
  com.klsjnh.web.dataservice011.vo.julydatasource.JulyDatasourceUpdateVo011   入参（password 留空即保持原值）
  com.klsjnh.web.dataservice011.vo.julydatasource.JulyDatasourceQueryVo011    分页入参（keyword / status）
  com.klsjnh.web.dataservice011.vo.julydatasource.JulyDatasourceTestVo011     测试连接入参（有 id 重测已存 / 无 id 草稿态）
  com.klsjnh.web.dataservice011.vo.julydatasource.JulyDatasourceTestResultVo011 测试连接出参

java17-infrastructure011（config）
  KrtConfig011                                                            ⏸ 本期未动：dataservice011.secretKey 留待二期加密存储时预埋
```

## 013.关键路径

### 013.1 表驱动注册表（本期技术核心）

现有 `DynamicDataSourceRegistryImpl` 的 `configs` 只从 `krt.ci011` 装载。本期改为**表为运行时真源**：

```text
启动 / reloadRegistry 调用
  → repository.findAllEnabled()            # july_datasource where dr='0' and status='1'
  → 逐条转 ConnectionInfo（driverClass 空则按 dbType 取 DatabaseType011 默认）
  → registry.reloadAll(List<ConnectionInfo>)
       ├── 先构建 nextMap（null / 空 dsCode 跳过），再整体替换 configs
       ├── Pass 1：快照内的 dsCode — 物理目标未变则 reused++ 保留旧池；
       │          变了且已建池则 DynamicDataSource011.remove + pools.close
       ├── Pass 2：旧声明集里快照中已消失的 dsCode（删 / 停用）→ 同样关池
       └── 回报 ReloadResult { enabled, registered, reused, closed, failed } 并落一条日志
```

**池的创建仍是懒加载**（`DataSourcePools011.get` 的 `computeIfAbsent`）—— `reloadAll` 只声明配置与关旧池，**不预建新池**。这是本项目优于老项目之处（老项目 `buildRegisteredPool` 内 `pool.init()` + `initialSize 2`，启动即连，一个库配置错就拖慢启动）。

**池保留判据**（落在 `ConnectionInfo.sameTarget(other)`）：`dsCode` 存在 **且** `jdbcUrl` / `username` / `password` / `dbType` 四值全等 → 复用；否则关旧建新（下次调用时懒建）。避免"改个 remark 就断所有连接"。

**两个 pass 不重叠**：Pass 1 处理"在快照里但变了"，Pass 2 处理"不在快照里了"，同一个 dsCode 不会被关两次。

### 013.2 测试连接（本期明确口径）

用户定案的四项参数全部落地：

| 参数 | 值 | 说明 |
|------|-----|------|
| `initialSize` | 0 | 不预热 |
| `maxActive` | 1 | 单连接探测 |
| `maxWait` | 10000 | 10s 超时 |
| 注册表 | **不碰** | 探测完即 `pool.close()`，注册表零副作用 |

```text
testConnection(vo)
  → vo 含 id        → 取库中配置（password 留空则用库里存的那份；支持重测已保存数据源）
  → vo 不含 id      → 由 vo 字段构造草稿 ConnectionInfo（支持未保存即测，老项目同款体验）
  → DataSourceProbe011.probe(info)          # 实现 domain 的 DataSourceProbePort
       ├── pool = buildForProbe(info)      # maxActive 1 / maxWait 10s / testOnBorrow
       ├── conn = pool.getConnection()
       ├── meta.getDatabaseProductName() / getDatabaseProductVersion()
       └── finally: pool.close()           # 探测池必关（Druid 持真实 socket）
  → 返回 ProbeResult { success, message, databaseProduct, databaseVersion }
  → HTTP 恒 200：success=false 表示"探测动作成功，只是目标不通"，由调用方读 success 分支
```

**与既有 `DynamicDataSourceRegistryImpl.testConnection(ConnectionInfo)` 的关系**：该方法保留（端口既有契约），其实现内部同样走 `pools.buildForProbe`，但**不返回产品信息**。本期的 `DataSourceProbe011` 是**新增的**、供管理端点用的探测件，返回更丰富的 `ProbeResult`，且两个入口都遵守"不回灌注册表"。

### 013.3 密码出站脱敏

收口点唯一：**`JulyDatasourceConverter.toVo()`**（web 层）。

- 域聚合 `JulyDatasource` 持有 `password`（业务必需：建池要用）；
- 转换到 `JulyDatasourceVo011` 时**不复制该字段**，`Vo011` 内**不定义 `password`**（不是置空，是根本没这个字段——从类型层面杜绝泄露）；
- `testConnection` 的入参 VO 可带 `password`（探测需要），但它不是响应类型。

### 013.4 请求前缀与包结构

```
domain.dataservice011          ← 业务域（与 domain.system011 平级，顶层）
application.dataservice011
infrastructure.dataservice011
web.dataservice011
```

**为什么是顶层 `dataservice011` 而非塞进 `system011`**：`system011` 的语义是「系统管理」（菜单 / 用户 / 角色 / 配置）；数据源管理是**数据服务**能力，且后续要承接业务建模（[033](033.topic-business-modeling.md) 的 `july_business_modeling`）成长为独立域。URL 前缀 `/klsjnh/dataservice011/**` 与包名对齐。

**Swagger 分组**（`SpringDocConfig011` 一命名空间一组）：新增 `dataservice011GroupedOpenApi()` —— group `dataservice011` / displayName `数据服务011` / `pathsToMatch("/klsjnh/dataservice011/**")`。**Controller 的 `@Tag` 与表注释统一口径**：`数据服务011 - 数据源管理`（与 DDL `COMMENT='数据服务011 - 数据源管理'` 一字不差）。

> 与 `system011` 的差异：现有 system011 各 `@Tag` 写作「系统管理 - 配置管理」（不带 `011`）。新域一律带域号，**不改存量**（存量改动属独立任务，需单独授权）。

## 015.DDL 与契约增量

### 015.1 DDL（新落 `docs/sql/july_datasource.sql`）

按 `docs/sql/july_config.sql` 的**列顺序规范**：`id → 业务字段 → status → 审计四列 → dr`。

与老项目 DDL 的修正：
1. 审计四列改 `BasePo` 口径（`create_time` / `update_time` / `create_by` / `update_by`）；
2. `id` 用 `VARCHAR(33)`（对齐 `base-entity-columns.sql` 的 32 位 UUID + 余量）；
3. 补 `KEY idx_ds_code_status (ds_code, status)`（注册表 reload 的查询模式）；
4. 补 `sort_order INT NOT NULL DEFAULT 9999`（2026-09-15：管理列表手工排序，PO 改继承 `BasePo011`；insert/update 可维护，列表按 `sort_order ASC, id ASC`）。

### 015.2 API 契约

**无信封 / 状态码增量** —— 全部复用 `013.api-contract.md` 现有六键信封与 `HttpCodeEnum011`。错误码映射：

| 场景 | 状态码 |
|------|--------|
| `dsCode` 重复 / 保留字 / 未知 `dbType` / `jdbcUrl` 非 `jdbc:` 开头 | 400 |
| `getById` / `getByCode` 不存在 | 404 |
| 停用的 `dsCode` 被路由 | 400 |
| 探测失败（连不上 / 认证失败） | **200 + `data.success=false`**（探测动作本身成功执行，结果在载荷内） |

> 最后一条是有意设计：`testConnection` 的语义是"**执行一次探测**"，探测动作成功即 HTTP 成功，连通与否是业务结果。老项目同款口径。

### 015.3 无 Flyway / Liquibase

本项目 DDL 走 `docs/sql/*.sql` 手工执行（现有 `july_config.sql` 等同一口径），本期不引入 migration 工具。

## 017.与老项目的取舍（哪些不照搬）

| 老项目做法 | 本期决策 | 原因 |
|------------|----------|------|
| `DynamicDataSourceUtil` 硬编码 `if ("mysql"…oracle…sqlserver)` | ❌ 不照搬 | 已有 `DatabaseType011` 枚举是驱动类名唯一真源 |
| 池 `setFilters("stat,wall,log4j2")` | ❌ 不照搬 | 对业务库强加 wall 过滤，跨库 DDL / 多语句场景易误伤（现有实现已刻意不加） |
| `buildRegisteredPool` 内 `pool.init()` + `initialSize 2` | ❌ 不照搬 | 启动即连，配置错一个库拖慢启动；本期维持懒加载 |
| `reloadAll` 时全量关旧建新 | ⚠️ 改造 | 改为"配置未变则保留旧池"，避免无关改动断连 |
| 密码明文随查询回吐 | ❌ 不照搬 | 类型层面剔除字段（§013.3） |
| 列名 `pass_word` | ❌ 不照搬 | **违约命名**（单词内部拆下划线）；改 `password`，对齐 `july_user.password` 与 `ConnectionInfo.password`，规约见 [016.coding-standards §7](../016.coding-standards.md) |
| `batchLogicDelete` 命名 | ❌ 不照搬 | 违反现行门禁"动词打头"（§6.3），改 `logicDeleteBatch` |
| `testConnection` 临时池不清 | ⚠️ 改造 | 补 `finally` 关池（§013.2） |
| `DbType`（`final class` + 静态常量） | ❌ 不照搬 | 已有 `DatabaseType011` **枚举**；方言分页保留在 `SqlDialect011` |
| `OperatorContext` 手工 set/clear | ❌ 不照搬 | 本项目已定案走 request attribute + `AuditMetaObjectHandler`（见 [016.topic-persistence §017](../infrastructure011/016.topic-persistence.md)） |
| `july_sql` / `july_model` 三表齐发 | ⏸ 移至 [033](033.topic-business-modeling.md) | 用户定案本期只做数据源；后续由 033 落地（两表**合并为一张** `july_business_modeling`，产物交 lowcode011） |

**保留借鉴**：`reloadAll` 的三计数日志、草稿态测试连接、`dsCode` 保留字 `master` 校验、`ExternalDatasourceOps` 的"临时池探测"思路。

## 019.任务拆解（2026-09-15 已落码）

| # | 任务 | 层 | 状态 |
|---|------|-----|------|
| 1 | 落 `docs/sql/july_datasource.sql` DDL | docs | ✅ |
| 2 | `JulyDatasource` 聚合 + `Repository` 接口 + `QuerySpec` | domain | ✅ |
| 3 | `ConnectionInfo` 扩字段；`DynamicDataSourceRegistryPort` 补 `reloadAll`；`ReloadResult` / `DataSourceProbePort` | domain | ✅ |
| 4 | `JulyDatasourcePo` + `Mapper` + `RepositoryImpl` | infra | ✅ |
| 5 | `DataSourcePools011`：probe 统一参数 + `isBuilt()` + driverClass 回退 | infra | ✅ |
| 6 | `DataSourceProbe011` 探测组件（实现端口，不回灌） | infra | ✅ |
| 7 | `DynamicDataSourceRegistryImpl` 改表驱动 `reloadAll`（含池保留判据） | infra | ✅ |
| 8 | `KrtConfig011` 扩 `dataservice011.secretKey` | infra | ⏸ 本期未动，留二期加密存储时一并预埋 |
| 9 | `JulyDatasourceUseCase`（CRUD + batch + test + reload） | application | ✅ |
| 10 | 6 个 `Vo011` + `Converter`（密码收口） | web | ✅ |
| 11 | `JulyDatasourceController` 9 端点 | web | ✅ |
| 12 | 门禁 + 编译（**编译 SUCCESS / 门禁 195 文件 0 违规**） | 全 | ✅ 编译 + 门禁已过；运行期 E2E 待启动服务后补 |
| 13 | 同步 `infrastructure011/017` 与 `docs/README.md` 台账 | docs | ✅ |

### 019.1 实际端点清单（与 §015.2 契约一致）

`/klsjnh/dataservice011/julyDatasource/v1` 下 9 个：`insert` / `update` / `logicDelete` / `logicDeleteBatch` / `getById`(GET) / `selectListByPage` / `testConnection` / `reloadRegistry`。

`getByCode` **无 HTTP 端点**（与 `JulyConfig` 同口径：按编码读是程序行为，不是管理动作）。

## 021.E2E 验收清单（Step 5 用）

1. `insert` 建一条 mysql 数据源 → 200
2. 同名 `dsCode` 再 insert → 400
3. `dsCode` 用 `master` → 400
4. `dbType` 传 `bogus` → 400
5. `jdbcUrl` 不以 `jdbc:` 开头 → 400
6. `update` 改 `remark` → 200，且**既有池不断**（同 dsCode 后续路由成功；reload 日志 `reused=1`）
7. `update` 改 `jdbcUrl` → 200，旧池被关，下次路由重连（`closed=1`）
8. `update` 时 `password` 留空 → 库里密码不变
9. `getById` → **响应无 `password` 字段**（类型层无该字段）
10. `testConnection` 传**未保存**的草稿配置（连不上）→ **200** + `data.success=false`
11. `testConnection` 传已保存的 `id`（能连）→ 200 + `data.success=true` + `databaseProduct`
12. `status=0` 停用后 `reloadRegistry` → 该 dsCode 从注册表消失
13. `logicDelete` → 后续 `getById` 404；注册表内该 dsCode 消失
14. `logicDeleteBatch` 部分成功 → `BatchDeleteResultVo011` 逐条回报（整批只 reload 一次）
