# 037.topic-storage-migration（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/037](../requirement011/037.topic-storage-migration.md)（业务规则）与 [infrastructure011/019](../infrastructure011/019.topic-storage-center.md)（存储中心架构底册）。
> 参照实现：`D:\aikbjava011\java17011\kj17-f016_20260719_a011`（**借鉴能力边界，不照搬类设计**，差异见 §021）。
> 结论：本项目**只缺管理面**——端口与适配器已在；本方案补齐「表驱动实例 + 桶/对象管理 + 在线编辑」。
> **状态：已编码并 E2E（2026-09-17）** —— `july_storage` 表驱动多实例 + 两控制器（julyStorage=实例+桶 / julyObject=对象）+ 在线编辑；六项决策全定案（预签名本期做）。连带修复：`BackupUseCase` 改注入 `StorageResolverPort`。

## 011.技术落点（类清单）

```
java17-domain011
  com.klsjnh.domain.storagecenter.JulyStorage                聚合根（storageCode 不可变 / provider / 连接配置 / presign 秒数）
  com.klsjnh.domain.storagecenter.JulyStorageRepository      仓储（findByCode / findAllEnabled / findPage / count）
  com.klsjnh.domain.storagecenter.JulyStorageQuerySpec       查询规格（keyword / provider / status）
  com.klsjnh.domain.storagecenter.StorageConnectionConfig    连接配置值对象（provider/basePath/endpoint/ak/sk/secure/bucket/presign）
  com.klsjnh.domain.storagecenter.StorageResolverPort        ★ 新增 Port：storageCode → ObjectStoragePort（缓存）
  com.klsjnh.domain.storagecenter.ObjectStorageFactoryPort   ★ 新增 Port：连接配置 → ObjectStoragePort
  com.klsjnh.domain.storagecenter.EditableTextPolicy         ★ 在线编辑策略（≤1MB / editorKind / contentType）
  （既有）ObjectStoragePort / ObjectStat                端口不动，按 §015.1 视需要增补方法

java17-infrastructure011
  com.klsjnh.infrastructure.storagecenter.entity.JulyStoragePo         extends BasePo011，@TableName("july_storage")
  com.klsjnh.infrastructure.storagecenter.mapper.JulyStorageMapper     extends BaseMapper
  com.klsjnh.infrastructure.storagecenter.repository.JulyStorageRepositoryImpl  extends BaseRepository（business column = storage_code）
  com.klsjnh.infrastructure.storagecenter.StorageResolver011          实现 StorageResolverPort（code→adapter，ConcurrentHashMap 缓存 + evict）
  com.klsjnh.infrastructure.storagecenter.ObjectStorageFactory011     实现 ObjectStorageFactoryPort（按 provider 造 adapter）
  com.klsjnh.infrastructure.storagecenter.LocalObjectStorageAdapter   ★ 改造：构造入参由 StorageProperties 改为 StorageConnectionConfig
  com.klsjnh.infrastructure.storagecenter.MinioObjectStorageAdapter   ★ 改造：同上
  com.klsjnh.infrastructure.storagecenter.StorageProperties           ★ 降级为「播种配置」（不再直接造 bean 适配器）
  com.klsjnh.infrastructure.storagecenter.seed.StorageSeed011         启动播种：yaml → july_storage（存在即跳，对齐 031 seed）

java17-application011
  com.klsjnh.application.storagecenter.JulyStorageUseCase          实例 CRUD + testConnection
  com.klsjnh.application.storagecenter.StorageBucketUseCase        桶 CRUD + testConnection
  com.klsjnh.application.storagecenter.StorageObjectUseCase        对象 list/page/stat/upload/download/remove/batch + readText/saveText

java17-web011
  com.klsjnh.web.storagecenter.controller.JulyStorageController   /klsjnh/storagecenter/julyStorage/v1（实例+桶）
  com.klsjnh.web.storagecenter.controller.JulyObjectController    /klsjnh/storagecenter/julyObject/v1（对象）
  com.klsjnh.web.storagecenter.converter.* + vo/*                  各 Vo011（AK/SK 出参打码 ******）
java17-app011
  StorageSeedRunner                                            启动播种（若不在 infra 内自驱）
```

> Swagger：沿用现有 `storagecenterGroupedOpenApi`（`pathsToMatch("/klsjnh/storagecenter/**")`），**不新增分组**。
> Mapper 包 `com.klsjnh.infrastructure.storagecenter.mapper` 需加入主类 `@MapperScan`。

## 013.关键机制

### 013.1 表驱动多实例（对齐 031/036 口径）

```
解析：storageCode → StorageResolverPort.resolve(code)
  → 缓存命中即返回
  → july_storage.findByCode(code)（缺失/停用 → 400）
  → StorageConnectionConfig（from 聚合）
  → ObjectStorageFactoryPort.create(config) → adapter
  → 入缓存
```

- `krt.storage-center.*`（yaml）降级为**播种来源**：启动把 default 实例写入 `july_storage`（存在即跳，绝不覆盖），此后**表为唯一运行时真源**；
- provider 取值统一 `StorageType011`（local011/minio011/cos011/tos011/oss011/s3011）；老 `LOCAL/S3` 映射：`LOCAL→local011`、`S3→s3011`。

### 013.2 适配器工厂（对现状的改造）

现状：`LocalObjectStorageAdapter` / `MinioObjectStorageAdapter` 是 `@ConditionalOnProperty` 的 **Spring bean**，直接读 `StorageProperties` → **单实例**。

改造：
1. 适配器构造入参改为 `StorageConnectionConfig`（与实例解耦，可多实例）；
2. `ObjectStorageFactory011` 按 `config.provider` 造 adapter（local011→Local、minio011/s3011/…→Minio/S3 系）；
3. 适配器**不再是单例 bean**，由工厂按需造 + Resolver 缓存；`default-type` 只决定播种实例的 provider。

### 013.3 在线编辑（迁 `EditableTextPolicy`）

- `readText(bucket, objectName)`：`stat` 校验 ≤1MB → `get` → UTF-8 解码 → 返回 `{content, editorKind, contentType, size}`；
- `saveText(bucket, objectName, content)`：`content` UTF-8 字节校验 ≤1MB → `put`（contentType 按扩展名）；
- `editorKind`：`.sql`→`sql`，其余→`markdown`；
- 策略落 **domain**（纯 Java），应用层调用。

### 013.4 秘密收口

`accessKey` / `secretKey` 属敏感：出参一律打码 `******`（对齐老 f016 口径，2026-09-17 修订；不再走「类型层剔除」）；`update` 留空保持原值。日志不打印明文。

## 015.DDL 与契约

### 015.1 DDL（新落 `docs/sql/july_storage.sql`，本项目列口径）

```sql
CREATE TABLE IF NOT EXISTS july_storage (
    id                     VARCHAR(33)  NOT NULL                COMMENT '主键',
    sort_order             INT          NOT NULL DEFAULT 9999   COMMENT '排序（越小越靠前）',
    storage_code           VARCHAR(60)  NOT NULL                COMMENT '存储实例编码（全局唯一，不可变）',
    storage_name           VARCHAR(100) NOT NULL                COMMENT '存储实例名称',
    provider               VARCHAR(20)  NOT NULL DEFAULT 'local011' COMMENT '存储类型（StorageType011）',
    base_path              VARCHAR(500) NULL                    COMMENT '本地根目录（local011 必填）',
    endpoint               VARCHAR(300) NULL                    COMMENT 'Endpoint（S3 系必填）',
    access_key             VARCHAR(100) NULL                    COMMENT 'Access Key',
    secret_key             VARCHAR(300) NULL                    COMMENT 'Secret Key（出参打码 ******）',
    secure                 VARCHAR(3)   NOT NULL DEFAULT '0'    COMMENT 'HTTPS（0 否 / 1 是）',
    default_bucket         VARCHAR(100) NULL                    COMMENT '默认桶',
    presign_expiry_seconds INT          NOT NULL DEFAULT 3600   COMMENT '预签名有效期（秒）',
    remark                 VARCHAR(300) NULL                    COMMENT '备注',
    status                 VARCHAR(3)   NOT NULL DEFAULT '1'    COMMENT '状态（0 停用 / 1 启用）',
    create_by              VARCHAR(33)  NULL                    COMMENT '创建人',
    update_by              VARCHAR(33)  NULL                    COMMENT '最后修改人',
    create_time            DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建日期',
    update_time            DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后修改日期',
    dr                     VARCHAR(3)   NOT NULL DEFAULT '0'    COMMENT '删除标记（0 正常 / 1 已删除）',
    PRIMARY KEY (id),
    UNIQUE KEY uk_storage_code (storage_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='存储中心 - 对象存储实例';
```

> 与老表差异：`id VARCHAR(33)`、审计四列本项目口径、`dr '0'/'1'`、`provider` 用 011 类型值；`create_date/modify_date/created_by/updated_by` 老口径**不照搬**。

### 015.2 端点与出入参

前缀：`/klsjnh/storagecenter/julyStorage/v1`（实例+桶）、`/klsjnh/storagecenter/julyObject/v1`（对象）。

| 端点 | 方法 | 说明 |
|------|------|------|
| julyStorage /insert · /update · /logicDelete · /logicDeleteBatch | POST | 实例 CRUD（code 不可变；secret 留空保持） |
| julyStorage /getById · /getByCode | GET | 实例点查（AK/SK 打码 `******`、`secure` 为 `"1"/"0"`、`createdBy/updatedBy/createDate/modifyDate`） |
| julyStorage /selectListByPage | POST | 实例分页（keyword/provider/status） |
| julyStorage /testConnection | POST | 按实例配置探活（HTTP 恒 200；载荷 `{success,bucketCount,basePath|endpoint,message}`，对齐老代码） |
| julyStorage /selectBucketList · /selectBucketListByPage | POST | 桶列表 / 分页（每行 `{bucketName,creationDate}`，对齐老 `listBuckets`） |
| julyStorage /getBucket | GET | 查桶（`{bucketName,exists}`；不存在 404） |
| julyStorage /insertBucket · /removeBucket | POST | 建桶（可选 region）/ 删桶（非空大声失败） |
| julyStorage /testBucketConnection | POST | 桶层探活（同 `{success,bucketCount,basePath|endpoint,message}`） |
| julyObject /selectObjectList | POST | 对象键列表（`List<String>`，prefix、recursive） |
| julyObject /selectObjectListByPage | POST | 对象分页（每行 `{key,bucket,size,lastModified,contentType}`，对齐老 `listObjects`；contentType 仅 local 有） |
| julyObject /statObject | GET | 对象元数据（size/lastModified/contentType） |
| julyObject /uploadObject | POST | multipart 上传 |
| julyObject /downloadObject | GET | 二进制流下载（**不走信封**，契约例外） |
| julyObject /presignObjectUrl | GET | 预签名 URL（local→`file:` URI；S3/minio→真实预签名） |
| julyObject /removeObject · /batchRemoveObject | POST | 删对象 / 批量删 |
| julyObject /readObjectText | GET | **在线编辑·读**（≤1MB） |
| julyObject /saveObjectText | POST | **在线编辑·写**（≤1MB） |

## 017.对现状的改造点（务必先做，否则多实例起不来）

| # | 现状 | 改造 | 风险 |
|---|------|------|------|
| 1 | 两个适配器 `@ConditionalOnProperty` bean，读 `StorageProperties` | 构造改 `StorageConnectionConfig`；由工厂造 | **低**（端口不变，仅构造入参） |
| 2 | 无 `july_storage` 表 | 新建 + 播种 | 低 |
| 3 | 无 Resolver/Factory | 新增端口 + 实现 + 缓存 | 低 |
| 4 | 无 `@MapperScan` storage.mapper | 主类补包 | 低 |
| 5 | 无下载/编辑端点 | 新增应用 + web | 低 |
| 6 | 端口缺 `testConnection`/`presignedGetUrl`/`recursive list` | 视 §023 决策增补 | 中（端口变更，需评估既有调用） |

## 019.任务拆解（**已完成，2026-09-16**）

| # | 任务 | 层 |
|---|------|-----|
| 1 | 落 `docs/sql/july_storage.sql` DDL + dev 建表 | docs/db |
| 2 | domain：`JulyStorage` / 仓储 / QuerySpec / `StorageConnectionConfig` / `StorageResolverPort` / `ObjectStorageFactoryPort` / `EditableTextPolicy` | domain |
| 3 | infra：`JulyStoragePo`/`Mapper`/`RepositoryImpl` + Resolver + Factory + 适配器改造 + `StorageSeed011` | infra |
| 4 | application：实例/桶/对象 三个 UseCase（含 readText/saveText/presignedUrl） | application |
| 5 | web：两个 Controller（`julyStorage`=实例+桶 / `julyObject`=对象）+ Converter + VOs（AK/SK 打码） | web |
| 6 | 装配：`@MapperScan` + 播种 Runner | app |
| 7 | 门禁 + 编译 + E2E | 全 |

## 021.与老项目的取舍（不照搬）

| 老做法 | 本期决策 | 原因 |
|--------|----------|------|
| `create_date/modify_date/created_by/updated_by` | ❌ 用本项目公共列口径 | 016 §7 命名规约 |
| `id varchar(32)` | ❌ `VARCHAR(33)` | base-entity 模板 |
| `dr varchar(1)` | ❌ `VARCHAR(3)` '0'/'1' | Status011/Dr 口径 |
| `provider LOCAL/S3` | ⚠ 改造为 `StorageType011` | 六值预留，与 019 底册一致 |
| 端口 `InputStream` 形态 + `Map<String,Object>` 返回 | ⚠ 保留本项目 `byte[]` + 记录类型；流式列二期 | 019 §021 二期 |
| `presignedGetUrl` | ✅ 迁（local→`file:` URI；S3/minio→minio SDK 预签名） | 决策 4 定案做 |
| `R`/`PageSizeResolver`/`PageQuery` 老通用体 | ❌ 用本项目 `Response011`/`PageQuery011`/`PageResult011` | 契约统一 |
| `DomainException` | ❌ 用 `BusinessException` | 全局异常口径 |

## 023.风险点

| # | 风险 | 等级 | 对策 |
|---|------|------|------|
| 1 | 适配器改造影响既有端口调用方 | 中 | 端口不变，仅构造入参 + bean 装配方式；既有 `ObjectStoragePort` 注入点需改为经 Resolver（若已有调用方） |
| 2 | 表驱动后 yaml 与表漂移 | 中 | 播种「存在即跳」+ 漂移 WARN（对齐 031 seed） |
| 3 | `accessKey` / `secretKey` 泄露 | 高 | 出参打码 `******` + 日志不打印明文 |
| 4 | 大对象下载 OOM | 高 | 本期管理面下载**先限大小**或直接列流式二期；见 §023 |
| 5 | bucket/object 操作打真实存储 | 中 | 本地盘/测试 MinIO 验证；生产用只读或独立实例 |

## 025.E2E 验收清单（执行时用）

1. `insert` 建一个 local011 实例 → 200
2. 同 `storageCode` 再 insert → 400
3. `getById` → AK/SK 打码 `******`、`secure` "1"/"0"、审计字段 `createdBy/updatedBy/createDate/modifyDate`
4. `testConnection`（local011）→ 200 `{success,bucketCount,basePath,message:"ok"}`；minio 回 `endpoint` 而非 `basePath`
5. `insertBucket` 建桶 → `selectBucketList` 可见（`{bucketName,creationDate}`）→ `getBucket` 命中（`{bucketName,exists:true}`）
6. `bucket/remove` 非空桶 → 明确失败；空桶 → 200
7. `object/upload` → `stat` 元数据（size/contentType） → `selectList` 可见
8. `object/download` → 二进制流（不走信封）
9. **`object/readText`** 对一个 `.sql` 对象 → `editorKind=sql` + content
10. **`object/saveText`** 改内容 → `readText` 回读一致；>1MB → 400
11. `object/remove` / `batchRemove` → 消失
12. minio011 实例 `testConnection` + 桶/对象同链路（Docker MinIO）
13. 停用实例 → 解析/调用 400
14. 日志**无 secretKey**；出参 AK/SK 打码 `******`
