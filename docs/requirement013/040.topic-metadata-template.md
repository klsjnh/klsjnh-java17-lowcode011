# 040.topic-metadata-template（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/040](../lowcode011/029.topic-metadata-template.md)。
> 定位：给元数据一个**声明式 JSON 模板**入口（取默认 / 导出 / 上传审核 / 部署），复用 038/039 能力，**不新增表、不改既有端点**。
> **状态：已实现并 E2E（2026-09-17）**。

## 011.技术落点（类清单）

```
java17-application011（包 com.klsjnh.application.lowcode011）
  JulyMetadataTemplateUseCase
    blankTemplate()                     默认骨架（4 段 + _guide + templateVersion）
    downloadTemplate(objectName)        导出（DesignerUseCase.load + metaData.source）
    reviewTemplate(template)            审核（校验 + previewDdl + plan，零副作用）
    deployObject(template, objectName, overwrite)  部署（校验 → 落库 → 发布）
    validate(template)                  校验 + 归一（基列类型归一、businessField/pk_* 强制 string(33)）
  （复用）JulyMetadataUseCase / JulyMetadataDesignerUseCase / JulyMetadataPublishUseCase
          MetadataDdlGeneratorPort / MetadataDdlExecutorPort

java17-web011（包 com.klsjnh.web.lowcode011.controller）
  JulyMetadataTemplateController
    GET  /getTemplate011
    GET  /downloadTemplate011?objectName=
    POST /uploadTemplate011   (multipart: part `file` | application/json body)
    POST /deployObject        { template | objectName, overwrite? }

java17-domain011
  JulyMetadataSource                   来源读模型（dataSourceCode / probeSql）
  JulyMetadataRepository               +updateSource(id, ds, sql) / +findSource(objectName)

java17-infrastructure011
  JulyMetadataPo                       +dataSourceCode / probeSql / sqlCode
  JulyMetadataRepositoryImpl           +updateSource / findSource（UpdateWrapper / QueryWrapper）
```

## 013.关键机制

- **模板 = MetaDTO + `source` + `templateVersion`（+ 可选 `_guide`）**：`downloadTemplate` 由 `designer.load(objectName)` 加 `metaData.source` 拼出；`deployObject` 反向映射回一主三子，复用 038 `insert/update`（三子整替）。
- **注释形式**：合法 JSON + `_guide` 字段（JW 不支持注释；不引入 JSONC 解析）。
- **校验与归一集中一处**（`validate`）：`reviewTemplate` 与 `deployObject` 共用，避免"审核一套、落库一套"。
  - 基列 `id/status/dr/create_by/update_by/create_time/update_time` **允许出现一次**，类型归一（33/3/date）；**再加同名（含大小写）= 子 code 大小写不敏感重名 → 拦截**。
  - `businessField` 必须存在且**不得为基列**，固定 `string(33)`；`pk_*` 固定 `string(33)`。
- **审核零副作用**：用 `MetadataDdlGeneratorPort` 对**内存 fields** 直接生成 `previewDdl`，用 `MetadataDdlExecutorPort.tableExists` 判 `plan=create|alter`；不落库、不执行。
- **部署**：校验 → 已存在则要求 `overwrite=true`（否则 400）→ 038 保存 → `updateSource` → 039 `publish`（受 `krt.lowcode.ddl-execute.enabled` 约束）。
- **无状态两拍**：`uploadTemplate011` 与 `deployObject` 之间无暂存；deploy 重新校验（幂等）。
- **双入口**：multipart 单文件与 JSON body 映射到同一 `reviewTemplate`（`consumes` 区分）。

## 015.端点与出入参

| 端点 | 方法 | 入参 | 出参 |
|------|------|------|------|
| /getTemplate011 | GET | 无 | 骨架模板 |
| /downloadTemplate011 | GET | `objectName` | 模板（1 主 3 子 + source） |
| /uploadTemplate011 | POST | multipart `file` 或 JSON | `{valid, errors[], warnings[], plan, previewDdl}` |
| /deployObject | POST | `{template?, objectName?, overwrite?}` | `{objectName, saved, published, version, physicalTable, ddl?}` |

## 017.改造点

| # | 改造 | 说明 |
|---|------|------|
| 1 | 不新增表、不改既有端点 | 纯新增 4 端点 |
| 2 | `JulyMetadataPo` 加 `dataSourceCode/probeSql/sqlCode` 字段 | 供 source 往返（表列本已存在） |
| 3 | `JulyMetadataRepository` 加 `updateSource/findSource` | 来源持久化 / 读取 |
| 4 | Swagger 组 `低代码011` 已覆盖 `/klsjnh/lowcode011/**` | 新端点自动进组 |

## 019.任务拆解（已完成）

| # | 任务 | 状态 |
|---|------|------|
| 1 | domain：`JulyMetadataSource` + 仓储两方法 | ✅ |
| 2 | infra：`JulyMetadataPo` 加列 + 仓储实现 | ✅ |
| 3 | application：`JulyMetadataTemplateUseCase`（blank/export/review/deploy + 校验归一） | ✅ |
| 4 | web：`JulyMetadataTemplateController`（4 端点，multipart/json 双入口） | ✅ |
| 5 | 门禁 + 编译 + E2E | ✅ |

## 021.E2E（2026-09-17）

见 [req011/040 §020](../lowcode011/029.topic-metadata-template.md)。要点：往返 `valid/plan=alter`；新对象 `plan=create`；追加业务 `status` 被 `duplicate` 拦截；multipart 通过；`deployObject` 建 `bpm_user_tpl`（含唯一键）且 `source` 持久化；`objectName` 直发 / `overwrite` 开关均符合预期。

## 023.风险

- **基列重名**：靠"子 code 大小写不敏感唯一"拦截（039 实测教训）。
- **overwrite 语义**：三子整替 + 子行物理删 → 元数据编辑历史丢失（与 038 一致）。
- **部署高危**：仍受开关 + DDL 白名单（首建 CREATE / 后仅 ADD COLUMN / 禁 DROP）；生产需 DBA 闸门。
- **批量**：本期只做单对象模板；批量 `templates[]` 未做（可后续加，注意发布不在事务内）。
