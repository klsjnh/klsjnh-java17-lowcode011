# 035.topic-dictionary（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/035](../requirement011/035.topic-dictionary.md)（业务规则，九项决策已定案）与 [infrastructure011/016](../infrastructure011/016.topic-persistence.md)（持久化基座 · 主子表）。

## 011.技术落点（类清单）

```
java17-domain011
  com.klsjnh.domain.system011.dictionary.JulyDictionary               聚合根（dictionaryCode 不可变 / sortOrder / Status011）
  com.klsjnh.domain.system011.dictionary.JulyDictionaryItem           明细实体（pkMt 主表链接 / itemCode 不可变）
  com.klsjnh.domain.system011.dictionary.JulyDictionaryRepository     主表仓储（findEnabledByCode / findPage / count）
  com.klsjnh.domain.system011.dictionary.JulyDictionaryItemRepository 明细仓储（findByMaster / findByMasterAndCode）

java17-infrastructure011
  com.klsjnh.infrastructure.system011.entity.JulyDictionaryPo          extends BasePo，@TableName("july_dictionary")
  com.klsjnh.infrastructure.system011.entity.JulyDictionaryItemPo      extends BasePo implements MasterLinked，@TableName("july_dictionary_item")
  com.klsjnh.infrastructure.system011.mapper.JulyDictionaryMapper      extends BaseMapper
  com.klsjnh.infrastructure.system011.mapper.JulyDictionaryItemMapper  extends BaseMapper
  com.klsjnh.infrastructure.system011.repository.JulyDictionaryRepositoryImpl
        extends BaseMasterSubRepository021（getChildServices 声明明细仓；提供 cascadeDelete / getWithChildren）
  com.klsjnh.infrastructure.system011.repository.JulyDictionaryItemRepositoryImpl
        extends BaseRepository

java17-application011
  com.klsjnh.application.system011.dictionary.JulyDictionaryUseCase   CRUD + 明细 + getByType/getLabel/isValidItem（@Transactional 只在此层）
  com.klsjnh.application.system011.dictionary.JulyDictionaryExportProvider      030 导出（objectCode = julyDictionary）
  （明细导出 JulyDictionaryItemExportProvider：属边界外，未实现）

java17-web011
  com.klsjnh.web.system011.controller.JulyDictionaryController        11 管理端点（含 /export、/getByCode）
  com.klsjnh.web.system011.converter.JulyDictionaryConverter          聚合 ↔ Vo011（主表 + 明细）
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryVo011          字典出参（含明细列表）
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryInsertVo011    字典入参（新增）
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryUpdateVo011    字典入参（修改；code 不可变）
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryQueryVo011     字典分页入参
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryItemVo011      明细出参
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryItemInsertVo011 明细入参（新增）
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryItemUpdateVo011 明细入参（修改）
  com.klsjnh.web.system011.vo.julydictionary.JulyDictionaryItemQueryVo011  明细列表入参（dictionaryCode + status + keyword）
```

> Mapper 包 `com.klsjnh.infrastructure.system011.mapper` 已被主类 `@MapperScan` 覆盖，**无需改装配**。
> Swagger：`system011` 组按 `pathsToMatch("/klsjnh/system011/**")` 已覆盖本模块，**无需新增分组**。

## 013.关键机制

### 013.1 主子表落地方式（与「替换式」的区别）

`BaseMasterSubRepository021` 的 `saveChildren` 是**替换式**（旧明细全逻辑删 + 重插），适用于「整单提交」。字典明细是**逐条增改删**的管理动作，故：

- 主表仓继承 `BaseMasterSubRepository021`，复用它提供的 `cascadeDelete` / `getWithChildren`（删除约束与联查）；
- 明细的增/改/删**直接走明细仓**（`insert` / `update` / `logicDelete`），**不使用** `saveChildren` 替换；
- 使用用例持有 `@Transactional`（跨主/子两次写必须同一事务）。

### 013.2 程序读入口（无 HTTP）

```
getByType(code)              → 主表 findEnabledByCode(code)；null（不存在/停用）直接返回 null
                             → 明细 findByMaster(id) 过滤 status='1'，按 sort_order 升序
getLabel(code, itemCode)     → getByType 后按 itemCode 命中，返回 itemLabel；否则 null
isValidItem(code, itemCode)  → getLabel != null
```

- 与 029 `getByCode` 同口径：**每次查库、无缓存**；
- 停用字典 → `getByType` 返回 null（对其明细同样不可见）。

### 013.3 排序与唯一

- 主表 `dictionaryCode` 唯一（`uk_dictionary_code`）；明细 `(pk_mt, item_code)` 唯一（`uk_pk_mt_item_code`）；
- 所有列表（管理分页 / 明细 / 程序读取）一律 `ORDER BY sort_order ASC`，同值按 `id` 稳定次序。

## 015.DDL 与契约

- DDL：`docs/sql/july_dictionary.sql`（主表 + 子表，列顺序 `id → 业务字段 → status → 审计四列 → dr`）。
- **无信封 / 状态码增量** —— 复用 `013.api-contract.md` 六键信封与 `HttpCodeEnum011`。
- 错误码映射：

| 场景 | 状态码 |
|------|--------|
| dictionaryCode 重复 / itemCode 同字典内重复 | 400 |
| getById / getLabel 目标不存在 | 404（getById）/ null（getLabel） |
| 删除主表时仍存在存活明细（默认口径） | 400 |

### 015.1 端点与出入参

| 端点（前缀 /klsjnh/system011/julyDictionary/v1） | 方法 | 请求 Body | 出参 data |
|--------------------------------------------------|------|-----------|-----------|
| /insert | POST | dictionaryCode / dictionaryName / sortOrder / remark | IdVo011 |
| /update | POST | id + dictionaryName / sortOrder / status / remark | IdVo011 |
| /logicDelete | POST | id | IdVo011 |
| /getById | GET | id（query） | JulyDictionaryVo011（含明细） |
| /getByCode | GET | code（query） | JulyDictionaryVo011（含明细） |
| /selectListByPage | POST | pageIndex / pageSize / keyword / status | PageResult011 |
| /insertItem | POST | dictionaryCode + itemCode / itemLabel / sortOrder / remark | IdVo011 |
| /updateItem | POST | id + itemLabel / sortOrder / status / remark | IdVo011 |
| /logicDeleteItem | POST | id | IdVo011 |
| /selectItemListByType | POST | dictionaryCode / status / keyword | List<JulyDictionaryItemVo011> |

> `insertItem` 以 `dictionaryCode` 定位主表（前端拿到的是编码，不是主表 id），服务端查主表后落 `pk_mt`。

## 017.与既有模块对齐

| 维度 | 对齐对象 | 说明 |
|------|----------|------|
| 逐条 CRUD + 程序读入口 | 029 julyConfig | getByCode ↔ getByType，均无 HTTP 读端点 |
| 主子表基座 | infrastructure011/016 | `BaseMasterSubRepository021` + `MasterLinked` + `pk_mt` |
| 导出 | 030 | 注册制 Provider，objectCode 各一 |
| 排序 | 011 menu / 013 organization | `sort_order` 升序 |

## 019.任务拆解（已完成，2026-09-15）

| # | 任务 | 层 | 状态 |
|---|------|-----|------|
| 1 | 落 `docs/sql/july_dictionary.sql` DDL | docs | ✅ |
| 2 | `JulyDictionary` / `JulyDictionaryItem` 聚合 + 两个仓储接口 | domain | ✅ |
| 3 | 两个 Po + 两个 Mapper + 两个 RepositoryImpl | infra | ✅ |
| 4 | `JulyDictionaryUseCase`（CRUD + 明细 + 程序读入口 + `getByIdWithItems` + `getByCodeWithItems`） | application | ✅ |
| 5 | `JulyDictionaryExportProvider`（objectCode `julyDictionary`；明细导出属边界外） | application | ✅ |
| 6 | 8 个 Vo011 + Converter + Controller **10 端点**（含 `/export`） | web | ✅ |
| 7 | 门禁 + 编译 + E2E（§021 验收） | 全 | ✅ |
| 8 | 同步 `docs/README.md` 台账 | docs | ✅ |

## 021.E2E 验收清单（Step 5 用）

1. `insert` 建一条字典（dictionaryCode=demo_dict）→ 200 + IdVo011
2. 同 `dictionaryCode` 再 insert → 400
3. `update` 改名称/排序/状态 → 200
4. `insertItem` 在该字典下建两条明细 → 200 + IdVo011
5. 同字典内重复 `itemCode` → 400；另一字典用同 `itemCode` → 200
6. `updateItem` → 200
7. `getById` → 返回字典 + 明细，明细按 sort_order 升序
8. `selectListByPage` 关键词 + 状态过滤 → 命中正确
9. `selectItemListByType` → 明细按 sort_order 升序、状态过滤生效
10. `logicDeleteItem` → 该明细消失，主表不受影响
11. 启用字典 + 启用明细 → `getByType` 返回有序明细
12. 明细 `status=0` → `getByType` 不含该明细；`getLabel`/`isValidItem` 返回 null/false
13. 字典 `status=0` → `getByType` 返回 null
14. `logicDelete` 主表时仍存活明细 → 400（默认口径）
15. 明细清空后 `logicDelete` 主表 → 200；`getById` 404
16. 导出 `julyDictionary`（030 端点）→ csv/json 正常；明细导出属边界外
