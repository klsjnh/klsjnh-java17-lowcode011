# 039.topic-lowcode-designer-runtime（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/039](../lowcode011/028.topic-lowcode-designer-runtime.md)。
> 定位：在 [038](../requirement013/038.topic-lowcode-core.md)（一主三子 CRUD）之上，补「设计 → 探测 → 发布建表 → 数据同步 → 运行时 → 开放 API」全链路；**分三期**，每期可独立演示/上线/回滚。
> **现状（2026-09-17 收口）**：一期设计（`listModels/load/save/previewDdl`）✅；二期发布（`publish`）+ 数据同步（`importDataFromSql/importStatus`）✅；三期开放 API ✅；运行时段（`publishMenu`/`listRuntimeMenus`/`initialize` + `/runtime` 运行页 + 权限模型）**挂起**。

## 011.技术落点（分期类清单）

```
java17-domain011（包 com.klsjnh.domain.lowcode011）
  发布快照
    JulyMetadataVersion              snapshot record（objectName/version/payloadJson/physicalTable/ddlText/publishStatus/publishedBy/publishedAt）
    JulyMetadataVersionRepository    仓储（insert / findLatest / findByObjectNameAndVersion / existsByObjectNameAndVersion / listByObjectName）
  DDL
    MetadataDdlGeneratorPort         generateCreate(table, comment, fields, businessField) / generateAddColumns(table, fields)（纯领域，无 IO）
    MetadataDdlExecutorPort          tableExists / columnsOf / execute(ddl)（主数据源；开关 krt.lowcode.ddl-execute.enabled）
  数据
    MetadataDataWriterPort           upsert(physicalTable, businessField, rows)（同步写；按业务键）
    MetadataDataAccessPort           select / count / insert / updateByKey / deleteByKey（运行时 / 开放 API 读写）
  开放 API
    JulyMetadataOpenApi              聚合（objectName/enabled/authMode/allowedOps/apiKey/apiKeyUpdatedAt）
    JulyMetadataOpenApiRepository    仓储（findByObjectName / insert / update）

java17-infrastructure011（包 com.klsjnh.infrastructure.lowcode011）
  entity/JulyMetadataVersionPo + mapper + repository      发布快照表（**不继承 BaseRepository**，append-only by object_name+version）
  entity/JulyMetadataOpenApiPo + mapper + repository      开放 API 配置表
  ddl/MySqlMetadataDdlGenerator     MySQL：CREATE / ADD COLUMN；业务键强制 VARCHAR(33) + UNIQUE KEY；标识符 ^[A-Za-z_][A-Za-z0-9_]{0,59}$
  ddl/DatasourceDdlExecutor         主数据源 JdbcTemplate；开关默认 false（暗发）
  sync/JdbcTemplateMetadataDataWriter  列名大小写不敏感映射 + 缺基列补默认 + 按业务键 upsert
  runtime/JdbcTemplateMetadataDataAccess  通用读写（标识符校验 + 全绑定 + dr 逻辑删）

java17-application011（包 com.klsjnh.application.lowcode011）
  JulyMetadataDesignerUseCase      listModels / load / save / previewDdl（MetaDTO 组装内聚于此）
  JulyMetadataPublishUseCase       publish（首建 CREATE / 后仅 ADD COLUMN / 快照 / 版本 / 指针）
  JulyMetadataDataSyncUseCase      importDataFromSql（分页读 → upsert）/ importStatus
  JulyMetadataOpenApiUseCase       getConfig / saveConfig / rotateApiKey
  OpenApiUseCase                   meta / query / insert / update / delete（api_key 鉴权）

java17-web011（包 com.klsjnh.web.lowcode011.controller）
  JulyMetadataDesignerController   listModels/load/save/previewDdl
  JulyMetadataPublishController    publish
  JulyMetadataDataSyncController   importDataFromSql/importStatus
  JulyMetadataOpenApiController    openApiConfig/saveOpenApiConfig/rotateApiKey
  OpenApiController                /klsjnh/open/v1（meta/query/insert/update/delete）
  GlobalAuthFilter                 白名单加 /klsjnh/open/（免 JWT）
```

> **未做（运行时段）**：`JulyMetadataRuntimeUseCase` / `JulyMetadataRuntimeController`（`publishMenu`/`listRuntimeMenus`/`initialize` + `/runtime` 运行页）+ 运行时权限模型；`JulyMetadataMetaConverter` 未单列（组装内聚在用例里）。

## 013.关键机制

- **MetaDTO 组装**：一主三子 ↔ 老 `MetaDTO` 形状的组装内聚在 `JulyMetadataDesignerUseCase`（load）与 `JulyMetadataPublishUseCase`（快照 payload），未单列 Converter；两处都从同一聚合读出，保持字段一致。
- **发布快照**：`publish` 把当次聚合落 `july_metadata_version`（含 `ddl_text`），并把 `july_metadata` 的 `publish_status=PUBLISHED` / `version` 指向该版本；**DRAFT 编辑不触碰已发布快照**。
- **DDL 生成器**：`previewDdl` 与 `publish` 共用 `MetadataDdlGeneratorPort`（`generateCreate` / `generateAddColumns`）；首次 `CREATE TABLE IF NOT EXISTS <objectName>`（业务键固定 `VARCHAR(33)` + `UNIQUE KEY uk_<table>_<field>`），其后仅 `ALTER ... ADD COLUMN`；**禁止 DROP/DROP COLUMN/MODIFY/RENAME**；标识符正则 `^[A-Za-z_][A-Za-z0-9_]{0,59}$` + 反引号转义。
- **发布幂等**：先判表在否（不在 → `CREATE IF NOT EXISTS`；在 → 仅补缺列 `ADD COLUMN`，天然幂等）→ 执行 DDL → 落快照(PUBLISHED) + 推进发布指针；失败可重跑（无 PENDING 断点续做）。开关 `krt.lowcode.ddl-execute.enabled` 默认 false。
- **探测复用（039 未单独暴露）**：探测走 `dataservice011` 现有 `probe` / `executeSql` 端点（只读 + maxRows + 字段推断 `inferFieldData`）；039 的 `save` 直接接收 MetaDTO。
- **数据同步映射**：源结果列 → 目标列**按列名大小写不敏感匹配**（`JdbcTemplateMetadataDataWriter`）；缺基列补默认（id 随机 UUID / status '1' / dr '0' / 时间 NOW）；按业务唯一键 upsert。因此**「业务字段映射平台基列」= 导入 SQL 带该列**即可（见 req011/039 §018），无需字段/代码改动；同名列（含大小写变体）不可再加元数据字段（唯一键大小写不敏感）。
- **新增业务列（A 路径）**：设计器 `save` 加字段 → `publish` 检测缺列自动 `ALTER ... ADD COLUMN` → 导入按别名映射；外键/引用列命名 `pk_<实体>` 且固定 `VARCHAR(33)`（见 req011/039 §018.1），全程无代码改动。
- **开放 API / 数据访问**：仅 PUBLISHED 对象；表/列以**库表目录为白名单**；值全绑定；`insert` 补基列默认；按元数据的 required/类型校验与**动态审计**（`object_code = objectName`）**已实现**。

## 015.DDL 与端点

- DDL 增量：`july_metadata` 加发布态列（`publish_status`/`version`/`physical_table`/`probe_sql`/`sql_code`/`data_source_code`/`data_initialized`/`last_sync_at`）；新增 `july_metadata_version`、`july_metadata_open_api`（一期一次到位）。
- 端点前缀统一 `/klsjnh/lowcode011/julyMetadata/v1`（对齐前端 `BASE=/klsjnh/lowcode011` + `actions.ts`）；运行时另有 `/runtime/<objectName>`；开放 API 另有 `/klsjnh/open/v1/<objectName>`。
- 分期端点见 [req011/039 §019](../lowcode011/028.topic-lowcode-designer-runtime.md)。

## 017.改造点

| # | 改造 | 说明 |
|---|------|------|
| 1 | `july_metadata` 加发布态列 + 建两张新表 | 一期 DDL |
| 2 | `@MapperScan` 加新 mapper 包 | 装配 |
| 3 | `SpringDocConfig011` 的 `lowcode011` 组已覆盖 `/klsjnh/lowcode011/**`，无需新增 | 文档 |
| 4 | 探测复用 `dataservice011`；DDL 执行走**主数据源**（非动态数据源） | 探测 / 发布 |

## 019.任务拆解（三期）

| 期 | # | 任务 | 状态 |
|----|---|------|------|
| 一 | 1 | DDL：`july_metadata` 加列 + `july_metadata_version` + `july_metadata_open_api`（+ dev 执行） | ✅ |
| 一 | 2 | domain：快照 record + 仓储接口 + `MetadataDdlGeneratorPort` | ✅ |
| 一 | 3 | infra：快照 PO/Mapper/Repo + `MySqlMetadataDdlGenerator` | ✅ |
| 一 | 4 | application：`JulyMetadataDesignerUseCase`（MetaDTO 组装内聚，未单列 Converter） | ✅ |
| 一 | 5 | web：`JulyMetadataDesignerController`（listModels/load/save/previewDdl） | ✅ |
| 一 | 6 | 门禁 + 编译 + E2E（`bpm_user011`：save → load → listModels → previewDdl） | ✅ |
| 二 | 7 | `MetadataDdlExecutorPort` + `DatasourceDdlExecutor`（开关 `krt.lowcode.ddl-execute.enabled` + 幂等） | ✅ |
| 二 | 8 | `JulyMetadataPublishUseCase`（publish：首建 CREATE / 后仅 ADD COLUMN / 快照 / 版本 / 发布指针） | ✅ |
| 二 | 9 | `JulyMetadataDataSyncUseCase`（`importDataFromSql` 分页读 → 目标 upsert / `importStatus`）+ web | ✅ |
| 二 | 10 | 门禁 + E2E（建表/ALTER/分页导入/幂等） | ✅ |
| 三 | 11 | 运行时动态 CRUD（白名单 + 绑定参数 + 元数据校验 `MetadataValueValidator`）✅；动态审计 ✅（`july_user_audit`，`object_code=objectName`） | ✅ |
| 三 | 12 | `publishMenu`/`listRuntimeMenus` + `/klsjnh/lowcode011/runtime/v1` 数据面 ✅；`initialize` ➖（表由迁移管理）；`/runtime` 运行页（前端）另行 | ✅ |
| 三 | 13 | 开放 API（apiKey 打码/轮换 + allowedOps + `/klsjnh/open/v1` 公共 CRUD + meta；限流待做） | ✅ |
| 三 | 14 | 运行时权限模型（对象级 + 操作级） | ⏳ 低优先 |
| 三 | 15 | 门禁 + E2E + 文档收敛 | ✅ |

> **取舍（2026-09-17 用户口径）**：核心链路已交付；行 14 的权限模型、真字段映射、限流、`syncData`、模板批量等**价值有限，保持现状、不再扩展**（见 req011/039 §022）。（行 11 的动态审计**已实现**，不在挂起之列。）

## 020.一期 E2E（助手自测，2026-09-17）

- 用例：数据源 `bpm011`（Oracle），SQL `select id, userid, name from tuser`，对象 `bpm_user011`。
- `POST /save`（MetaDTO：10 字段 / 3 显示 / 4 服务）→ 200 + id；
- `GET /load?objectName=bpm_user011` → metaData + 10/3/4 三子；
- `GET /listModels` → 含 `bpm_user011`（publishStatus=draft）；
- `GET /previewDdl?objectName=bpm_user011` → `CREATE TABLE IF NOT EXISTS bpm_user011(...)`（PK=id、类型映射正确）。
- **遗留（探针）**：033 探针带出伪列 `KLSJNH_RN`（行号）且不回别名（`id sid` 仍得 `ID`）——039 侧保存前手工剔除；建议 033 修复。

### 二期（发布）E2E（2026-09-17）

- `POST /publish`（bpm_user011）首次 → `CREATE TABLE IF NOT EXISTS bpm_user011(...)`，version `0.0.1`；表建成（10 列，PK=id，类型映射正确）；
- 再次 publish → 表已存在且无缺列 → `ddl=null`、version `0.0.2`（幂等）；
- 加字段 `email` 后 publish → `ALTER TABLE bpm_user011 ADD COLUMN email VARCHAR(100)`、version `0.0.3`（**后仅 ADD**）；
- `july_metadata` 指针 `PUBLISHED / 0.0.3 / bpm_user011`；`july_metadata_version` 三条快照（0.0.1/0.0.2/0.0.3）。
- **开关**：`krt.lowcode.ddl-execute.enabled`（dev=true；默认 false，生产暗发）。
- **修正**：`JulyMetadataVersionPo` 继承 `BasePo011` 需 `sort_order`，两张新表已补列（DDL 文件同步）。

### 二期（数据）E2E（2026-09-17）

- **业务键**：`businessField`（口径见 [045](045.topic-business-field.md)；第三方元数据历史上常用 `sid`）；保存时**归一为 `VARCHAR(33)`**，生成器兜底同规则；`CREATE` 附 `UNIQUE KEY uk_<table>_<businessField>`（`businessField=id` 时不另建）。
- **导入**：`importDataFromSql` 走 033 `executeSqlByPage`（方言分页）读源，`JdbcTemplateMetadataDataWriter` 按 `sid` 唯一键 `ON DUPLICATE KEY UPDATE` upsert；缺基列自动补默认（id 随机 UUID / status '1' / dr '0' / 时间 NOW）。
- **E2E**：page1 → 100 行；**page1 重导 → 仍 100（幂等）**；page2 → 200；page1 再导 → 仍 200；`mode` init→sync；`importStatus` `dataInitialized=true`。
- **已知简化**：`updated/unchanged` 暂以 processed 近似（未区分），`syncData`（非分页全量）暂以分页循环代替。

## 021.风险（已评审，见 req011/039 §023）

- **DDL 不可逆/半开**：幂等 + 可重入 + 开关 + 独立库/前缀；禁 MODIFY/DROP。
- **注入**：表/列/排序白名单 + 全绑定参数；`escapeIdentifier` 仅标识符。
- **权限/合规**：生产 DDL 走开关（默认关）+ DBA 审核；运行时权限模型单列。
- **探测慢查询**：只读 + 超时 + maxRows + EXPLAIN 预检。
- **version 语义**：对齐老 `version011`（`"0.0.1"` 递增字符串），前端占位递增整数需同步改。
- **E2E 污染**：独立测试库 + 对象名（物理表名）隔离。
