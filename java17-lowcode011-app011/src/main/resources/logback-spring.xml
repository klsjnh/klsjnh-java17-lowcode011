<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 日志目录可用 logging.file.path 覆盖，默认 ./logs -->
    <springProperty scope="context" name="LOG_PATH" source="logging.file.path" defaultValue="./logs"/>
    <property name="LOGGER_FILE" value="java17-app011"/>
    <property name="ENCODING" value="UTF-8"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%yellow([%date{yyyy-MM-dd HH:mm:ss}]) %highlight(%-5level) [%thread] %green(%logger) %msg%n</pattern>
            <charset>${ENCODING}</charset>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOGGER_FILE}.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOGGER_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>9999</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>[%date{yyyy-MM-dd HH:mm:ss}] %-5level [%thread] %logger %msg%n</pattern>
            <charset>${ENCODING}</charset>
        </encoder>
    </appender>

    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOGGER_FILE}-error.log</file>
        <append>true</append>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOGGER_FILE}-error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>9999</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>[%date{yyyy-MM-dd HH:mm:ss}] %-5level [%thread] %logger %msg%n</pattern>
            <charset>${ENCODING}</charset>
        </encoder>
    </appender>

    <!-- 生产：仅落文件，不输出控制台，框架日志收至 INFO -->
    <springProfile name="production">
        <logger name="com.klsjnh" level="INFO"/>
        <logger name="org.springframework" level="INFO"/>
        <logger name="com.baomidou" level="INFO"/>
        <logger name="com.zaxxer" level="INFO"/>
        <root level="INFO">
            <appender-ref ref="FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
    </springProfile>

    <!-- 非生产（development / local）：控制台 + 文件，框架日志 DEBUG -->
    <springProfile name="!production">
        <logger name="com.klsjnh" level="DEBUG"/>
        <logger name="org.springframework" level="INFO"/>
        <logger name="com.baomidou" level="INFO"/>
        <logger name="com.zaxxer" level="INFO"/>
        <logger name="org.quartz" level="INFO"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
    </springProfile>

</configuration>
