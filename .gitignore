# ---- build ----
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# ---- IDE ----
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.code-workspace
.settings/
.project
.classpath

# ---- OS（Windows / WSL / macOS 混合开发）----
.DS_Store
Thumbs.db
desktop.ini
*~

# ---- runtime storage (local011 adapter) —— 根锚定，避免误伤同名 Java 包 storage011/ ----
/storage011/

# ---- logs & temp ----
*.log
logs/
tmp/
*.tmp

# ---- runtime pid / tool state ----
*.pid
.workbuddy/

# ---- 本地私有配置（application.yml 公共结构正常提交）----
# 通配忽略所有环境 yml，仅 application-development.yml（开发共享）入库；
# application-local.yml（本机私有覆盖）与 application-production.yml 均被下面的通配规则忽略
application-*.yml
application-*.yaml
!application-development.yml
*.local

# ---- node（tools/*.mjs 只用 Node 标准库，防误装依赖）----
node_modules/

# ---- 本地备份（不入库）----
.backup011/

# ---- deploy runtime (logs/data/storage) ----
deploy/runtime/
deploy/java011/
